# Concord — Master Development Plan

Decentralized communication platform. The stable Matrix-based build lives at the repo root (currently `v0.1.0`, tagged, deployed on orrgate). The native Rust/Tauri + libp2p mesh rewrite lives in its own separate repository at `/home/corr/projects/concord_beta/` (extracted 2026-04-07 via commit 56cf2f9 — the `beta/` subdirectory in the root repo is now a near-empty stub). Both tracks share this roadmap while they converge, but they have independent commit histories.

## Open Conflicts

### From 2026-04-11 Feedback — All Resolved
1. ~~Phantom nodes~~ → **RESOLVED**: Phantom nodes are read-only scouts. They CANNOT cluster (no compute contribution). Disposable anonymous nodes CAN cluster and contribute compute. Phantom = observe only, anonymous = participate without identity.
2. ~~Charter immutability~~ → **RESOLVED**: Both mechanisms — (a) reject updates not signed by current owner AND (b) monotonic version counter. Belt and suspenders.
3. ~~Forum map-view rendering~~ → **RESOLVED**: WebGL / OpenGL. Enables 3D capabilities for future mesh map explorer and Game Center viewport. Game Center should also use a 3D-capable renderer.
4. ~~iOS deployment~~ → **RESOLVED 2026-04-11, revised 2026-04-08**: Testing path = AltStore/Sideloadly (free, re-sign every 7 days, no Apple Developer account needed). Production/App Store path = Apple Developer Program — user **enrolled** 2026-04-07, awaiting Apple ID verification (up to 48h). Both paths coexist: sideload for rapid iteration builds, App Store for paid native mobile distribution under INS-021. See Open Conflicts > "From 2026-04-22 Routing — Resolved" item 3.

### From 2026-04-06 Routing — Resolved
1. **INS-NNN ID collision**: Two separate instruction batches routed on 2026-04-06 both used IDs INS-001/002/003 in the inbox. The morning batch (mobile UI, markdown rendering, auto-grow input) kept the original IDs because they had already been implemented. The later batch (channel/server sidebar UX, 7 instructions) was renumbered INS-001B through INS-007B inside PLAN.md to preserve traceability without clobbering the completed entries. Future routings should check the existing PLAN.md for ID reuse and suffix or offset as needed.

### From 2026-04-07 Routing — Regression Suspected
1. **INS-008/009/010 duplicate INS-001/002/003 acceptance criteria verbatim**: The 2026-04-07 routing batch contains three instructions describing the *same defects* as the 2026-04-06 morning batch (mobile UI logout/scroll/wrap/menu, markdown rendering, auto-grow input + reflow). All three were marked shipped + tester-verified on 2026-04-06 (see Recent Changes). User has re-reported them, which means **either** (a) the prior fixes regressed in subsequent commits, **or** (b) the user is testing on a stale build / cached client and the deployed version does not match `client/src/`, **or** (c) the fixes did not actually solve the user-perceived problem despite passing tester agents. **Action required before re-implementing:** verify the live deployment on orrgate matches current `client/src/`, hard-refresh client cache, then re-run the original acceptance walkthroughs on a real mobile device. Treat INS-008/009/010 as **regression-investigation tasks**, not greenfield work, until reproduction is confirmed.

### From 2026-04-12 Feedback — Resolved 2026-04-08
1. ~~**Tunnel architecture choice**~~ → **RESOLVED**: **Hybrid** of both approaches. Charter-minted tunnels for mesh-integrated scenarios AND sidekick container nodes for per-user VPN hosting. They are complementary, not exclusive. Implementation lands in the concord-beta mesh track; root concord (Matrix stable) continues to use federation only.
2. ~~**Cut-off sentence — game dev workflow**~~ → **RETAINED AS FEATURE 2026-04-08**: User clarified — "I didn't intend to cut that feature off. This needs to be in the plan." The original cut-off sentence is no longer load-bearing (no missing details to chase down), but **the game dev workflow itself is a real planned feature** and now lives as an unchecked item in the concord-game-maker track under Game Center. Earlier "ignore it" referred to chasing the literal truncated sentence, not dropping the feature.
3. ~~**Cut-off sentence — AI image gen**~~ → **RETRACTED**: User removed the image-gen idea from the roadmap ("Whenever I said this I don't like it. Take it off."). Game Center roadmap updated: the "Integrated generative AI nodes (connect to model APIs + image gen)" line item removed from concord-game-maker.
4. ~~**Visual code editor selection**~~ → **RESEARCH ASSIGNED**: Recommend a system natively compatible with current stack (Tauri v2 + React/TS + Rust). Tracked as a concrete task under Remaining Design Work below; PM agent will assign.
5. ~~**Geological addressing**~~ → **MOVED TO concord-beta**: Meshes are strictly concord-beta's domain ("meshes are a strictly concord-beta's domain. That instruction should be put there instead."). The deterministic-addressing-with-geolocation question belongs to the libp2p mesh track, not the Matrix stable client. Removed from root PLAN.md; must be carried over to the concord-beta repo's planning document when that repo is next worked on.
6. ~~**Orrtellite subgroups justification**~~ → **DEFERRED**: User asked for the justification to be written AFTER all 2026-04-22 priority work lands. Captured as the last item in Remaining Design Work below. Non-blocking.
7. ~~**Orrtellite function boundary audit**~~ → **COMPLETE (2026-04-08)**: **Clean. No contamination.** Root concord repo (Matrix stable client) contains ZERO `orrtellite`, `tailscale`, `wireguard`, or `headscale` code references. All mesh/tunnel code was extracted to the concord-beta repo on commit 56cf2f9 ("chore: extract beta/ prototype to its own repository"). The only files that mention these terms are `PLAN.md`, `fb2p.md`, and `naming-brainstorm.md` — all as roadmap/discussion context, not code. No boundary violations to fix in this repo.

### From 2026-04-21 Routing — Incorporated (no collisions)
INS-011 through INS-019 routed in from `instructions_inbox.md` (source idea: `orrchestrator/plans/2026-04-21-01-59.md`). ID-collision check passed — IDs INS-011..INS-019 are unused elsewhere in PLAN.md, so no `C` suffixing required. Grouped into five coherent feature sections under Feature Roadmap below: (a) Top-Bar Utility Relocation + Mobile Compaction (INS-011, INS-016, INS-017), (b) Unified Settings Interface (INS-012), (c) Native Application Delivery milestone (INS-014, INS-015), (d) Chat input + mobile keyboard polish (INS-013, INS-018), and (e) OpenClaw visual presentation authoring (INS-019). One genuinely new open question raised — see below.

1. ~~**OpenClaw definition gap (INS-019)**~~ → **RESOLVED 2026-04-21** (user): OpenClaw is an **agentic AI harness** that runs a chatbot persona inside Concord. It is reachable from orrion via `ssh corr@openclaw`. INS-019b is therefore unblocked — the authoring affordances should be implemented inside the OpenClaw harness (not inside the Concord client), exposing a chart-emitting tool/function that produces `ChartAttachment` messages on the wire. The Concord client only needs the renderer + attachment-persistence half (INS-019a), which was already unblocked. Cross-project note: orracle manages AI agent personas workspace-wide; user described OpenClaw as its own "harness" so it may be standalone rather than orracle-hosted — verify by inspecting `corr@openclaw` before assuming overlap with orracle's tool-invocation pipeline.

### From 2026-04-22 Routing — Resolved 2026-04-08
All eight items from the INS-020..INS-023 batch (source: `orrchestrator/plans/2026-04-22-01-27.md`, user-labeled "CONCORD - TOP PRIORITY") were resolved by the user in one pass on 2026-04-08. INS-021 is now unblocked to enter the "PM drafts proposal" stage; INS-020/022/023 are unblocked for scaffolding.

1. ~~**Scope tag: `public` → `commercial`?**~~ → **RESOLVED**: Retagged to `commercial`. `.scope` file updated. Triggers the commercial profile (full CI/CD, dependency license audit, SLA-grade error handling, input validation on endpoints, test file per new module, proprietary-license-header option). Commercial posture applies to the whole Concord project, not just the native-app distribution channel.
2. ~~**v3 commerce deferral vs. OPT-002 narrow exception**~~ → **RESOLVED**: Commerce remains **low priority** overall — decisions about the broader economy come later when the app is ready for them. A narrow exception holds for the native mobile builds: **basic monetization via ads + an ad-free tier** is permitted pre-v3. The user framed this as "a plea for donations" rather than a paywall — the native apps are a cosmetic layer over a great web application. People who care pay, people who don't, don't. That's fine. **Every functional capability of Concord stays free via the browser-accessible web UI.** The v3 Scope Boundary stays in place for ALL other commerce (marketplaces, in-game economies, etc.).
3. ~~**Apple Developer Program enrollment**~~ → **RESOLVED**: User has **already enrolled**. Apple is verifying ID (up to 48h from 2026-04-07, i.e., may land by end-of-day 2026-04-09). Proceed as if enrolled — iOS paid distribution path via the App Store is viable. Do not treat the Apple Developer Program fee or account provisioning as a blocker.
4. ~~**Cross-platform framework choice**~~ → **RESOLVED**: Default to **Tauri v2** for everything (stable, beta, mobile). If the Tauri framework is necessary to implement proper cross-platform interaction it IS necessary — only explore Flutter/React Native alternatives if Tauri v2 hits a hard wall on a specific platform with no workaround. User direction: **"Don't break something that works, but we also need to explore other options. Just do the instructions."** No speculative framework rewrites; agents may experiment with alternatives only when a concrete Tauri v2 blocker is documented.
5. ~~**Mobile servitude vs. headless servitude — replace or coexist?**~~ → **RESOLVED**: **Single embedded architecture.** Servitude lives INSIDE the Concord app itself as a module — not a separate daemon, not a separate binary. Both mobile and desktop Concord builds are capable of hosting a stable Concord room with customization options. **The Concord mobile package includes an always-online embedded server that runs whenever the user is online.** Concord instances can ALSO connect to someone else's external headless servitude instance. This answer also affects the `concord-daemon → servitude` rename: rather than renaming a standalone binary, servitude is promoted to an internal module of the Concord app. INS-022 collapses from "port daemon to iOS" to "expose the already-embedded servitude module via a mobile UI toggle" — dramatically simpler. See rewritten INS-022 below and revised "Service Node Mode" section.
6. ~~**Feature parity floor for native frontends**~~ → **RESOLVED**: Option **(c) — full web-client feature parity** on native frontends. No capability deltas between web and native. Native is a polished UX over the same surface, not a reduced feature set.
7. ~~**Monetization model specifics**~~ → **RESOLVED 2026-04-08 (donation-only)**. User reviewed `client/MONETIZATION_PROPOSAL.md` and directed a full reframe under a donation-only model. Key decisions: (a) **in-house house ads** — no AdMob, no tracking, no third-party SDKs, (b) **no paid tier at all** — every feature free on every platform, (c) **three-way choice on first launch + in settings**: pay-what-you-want donation ($0.50–$49.99 consumable IAP ladder, default highlighted at $2.99) / remove-ads-for-free (one tap, no payment) / keep-ads-on (explicit opt-in, framed as supporting Concord via community-promotion surface), (d) **regional pricing doesn't apply** — handled automatically by the store IAP localization on per-SKU basis, (e) **refunds honored no-contest** per user directive ("They give me money or they do not. No pressure or way to trap them."), (f) **donation IAPs are consumable**, not non-consumable — donations are thank-you gifts, not unlocks; the ad-free flag is free for the asking regardless of payment. One residual open item: **crash reporting (opt-in Sentry vs BugReportModal-only)** — deferred to Phase 1. See revised `client/MONETIZATION_PROPOSAL.md` for full detail.
8. ~~**Mobile servitude NAT traversal strategy**~~ → **RESOLVED**: **All of the above.** The commercial version of Concord uses WireGuard + mesh networks + tunnels + the Matrix federated network together to connect users. This applies to servitude in every form factor. Servitude gets installed on an always-on device (desktop/headless on always-on infra OR the user's mobile while Concord is in foreground). Concord mobile spawns a servitude instance within its own bounds. Transport selection is dynamic and layered — use whatever works, not one exclusively.

### Remaining Design Work
*Mesh map mathematical framework, call ledger lifecycle, engagement profile all implemented 2026-03-27. New design work captured below.*

1. ~~**Mesh map mathematical framework**~~ — IMPLEMENTED. HMAC-SHA256 deterministic addressing, 4-tier confidence system, LWW merge protocol, Dijkstra path-of-least-action routing, 3-level locale partitioning (region/cluster/subnet).
2. ~~**Call ledger lifecycle**~~ — IMPLEMENTED. UUID-addressed entries: Active → Concluded → Tombstoned (5min grace). No mesh archival, local-only call history.
3. ~~**Engagement profile**~~ — IMPLEMENTED. Raw counters → posting ratio → tanh-compressed [-10,+10] score. Privacy: only score published, raw counters local-only.
4. ~~**Visual code editor recommendation**~~ → **RESOLVED 2026-04-08 (Blockly primary, Rete.js fallback)**. User accepted the primary recommendation from `docs/visual_code_editor_recommendation.md` on 2026-04-08 ("I like that primary recommendation"). Blockly (Apache-2.0, Raspberry Pi Foundation, five built-in codegens for JS/Python/Lua/Dart/PHP) is the primary pick; Rete.js core (MIT, best React/TS integration) is the fallback if Blockly's aesthetic proves wrong for the target audience. The text↔visual round-trip requirement is acknowledged as substantially unmet by the ecosystem and explicitly deferred — `concord-game-maker` will author games in a Blockly-defined DSL with generated code as a read-only export. Four unblock options (license Rete Studio / clean-room reimplement / accept DSL-only / wait for ecosystem) remain available in §6 of the recommendation doc if the round-trip requirement is ever reopened. User also clarified 2026-04-08 that the project is FoSS with donation-only mobile monetization, which relaxes but does not eliminate the license analysis (Rete Studio's CC BY-NC-SA 4.0 remains blocked for store-distributed apps; Flyde's AGPLv3 is now "still-not-recommended for propagation reasons" rather than an absolute commercial-license blocker — not switching in). Belongs to the **concord-game-maker** track under Game Center.
5. **Orrtellite subgroups justification** (from 2026-04-12 item 6, deferred 2026-04-08) — Document the rationale for personal-network subgroups before any ACL design work. Questions to answer: why are subgroups worthwhile? what use cases does ACL-without-subgroups fail to serve? how does the subgroup model interact with the charter/place model? **Write AFTER all 2026-04-22 priority work lands.** Non-blocking on current sprint; captured here so it is not lost.

## Resolved Decisions (2026-03-27)

| Topic | Decision |
|-------|----------|
| **Web portal domain** | `*.concorrd.com` (PLACEHOLDER — must be reassessed before distribution) |
| **Probe technology** | NOT containerized sandboxes. Probes are simple ping operations for node verification. See Node Verification Protocol below. |
| **"Confirmed human"** | One reputational tag among many in a merit-badge reputation system. See Reputation System below. |
| **Ledger compaction** | Zero-loss compression. Never delete server history. Media content excluded from ledger body, but filenames and transfer records are preserved. |
| **Forum hop scope** | Configurable, default 2 hops. |
| **Mobile quick actions** | Reconnect to last channel, host exchange, open profile customization, node settings. |
| **Launch games** | Chess, Checkers, Mafia, Poker (all kinds), Trivia (all kinds), Pictionary, Telestrations, Scrabble (8+). |
| **LLM models** | Flexible — any API (Claude, Gemini) or local Ollama. Game dev pipeline determines how to power LLM elements per-game. |
| **Game engine paradigm** | Flow-based visual code editor capable of making anything. Purpose-built nodes for point-and-click and text-controlled game dev. |
| **Service nodes** | Not a separate "mode" — same node representing a place instead of a user. Provides persistent infrastructure. Dynamic workload adjustment. Admin-approved via ledger appendix. Web admin UI. Managed by **servitude** app. |
| **Service node auth** | Same as any node. Admin mints appendix to whitelist service node. Service node is as trustworthy as the place. |
| **Additional performance** | User nodes can act as "support server" for another connection — separate from headless server app. |

## UX Language (defined 2026-04-11)

Canonical terminology for Concord. All UI, docs, and code should use these terms consistently.

| Term | Definition |
|------|-----------|
| **Forum** | The public space around a node on the mesh map. Has a **map view** (speech bubbles from chatting nodes) and a **chatroom view** (messages within forum scope). Forum size = configurable connection layers deep. Users can customize their forum layout; others see it when viewing that section of the map. Essentially a customizably-cropped subsection of the mesh map. |
| **Place** | A cluster charter minted at a fixed mesh-map location. Nodes gather here to form clusters. Foundation of the communication UX. |
| **Charter** | An immutable record encoded into the mesh map with all config for cluster formation. Only way to transfer ownership is to mint a fresh charter with the old charter's data compressed in. Maps to `PlacePayload` but must enforce immutability. |
| **Ledger** | Section of a charter containing complete interaction history. Feeds the reputation system. Can be disabled by admin. |
| **Cluster** | 2+ nodes as hot-swappable compute modules working together per a charter's rules. |
| **Tunnel** | VPN bridge making two nodes work as if on LAN. Implemented via WireGuard (orrtellite). |
| **Service Node** | Persistent node minted to a particular place. Low overhead, pulls more than its weight. (Formerly "server" — renamed to avoid Discord UX comparison. The word "server" confuses the value proposal by drawing a direct comparison to Discord.) |
| **Servitude** | The native application that manages service nodes. Headless daemon for persistent infrastructure. |
| **Node** | Independent Concord instance that can cluster with others to form chat rooms. |

**Naming Convention**: All naming decisions inside Concord must align with the theme **"single word descriptions of various social contracts"** (e.g., charter, servitude, cluster, trust, forum, place).

## Communication Flow (from 2026-04-12 feedback)

**Strict separation between comms and tunnels:**
- **P2P handshakes** (mesh-clustered) manage ALL actual communications (text, voice, video, file exchange)
- **NET tunnels** (WireGuard/orrtellite) ONLY serve to place machines close enough together virtually that the P2P protocol can operate at long distances
- **Local mesh connections and NET tunnels work side by side** — they are complementary, NOT replacements for each other. Losing the internet just reduces range.
- This dual-protocol approach is what makes Concord "the most reliable chat platform" — it operates on multiple protocols at once.

**Tunnel architecture** (two proposed approaches — Open Question):
1. **Charter-minted tunnels**: Tunnels integrated with mesh-map as minted charters near the node's home address. Charter dictates exclusivity, destination visibility, hashed secrets, credentials. Any node with access can continue downstream (chain routing). Default: public, unrestricted, anonymized.
2. **Sidekick nodes**: Small per-user nodes hosting VPN tunnel instances per charter spec. Requests routed via chains of containerized VPN connections. Concurrent sidekick count = live traffic density indicator.

**Orrtellite integration**: Orrtellite package used as a dependency (or copied/repurposed) to power NET tunnels. End users do NOT run an external tunneling service — orrtellite protocols are baked into Concord. The user's personal orrtellite instances are separate and do NOT assist Concord.

**Design philosophy**: Charter/node/cluster architecture is a hybrid of crypto ledgers, Kubernetes compute clusters, and decentralized mesh networks → zero-trust decentralized public digital communications protocol. Given enough adoption, this provides a worldwide decentralized communications framework.

## v3 Scope Boundary (from 2026-04-12 feedback, narrow exception added 2026-04-08)

**Commerce features are explicitly deferred to v3.** No advertising, marketplaces, or economy-stimulating features in v2. The current featureset UX must be perfect first. Concord must be a **tool first**. If social-media-like features emerge from public decentralized communication, that is a feature, not a bug — but do not design for it.

**Narrow exception (2026-04-08):** Basic monetization of the **native mobile frontend apps only** — ads + an ad-free tier — is permitted pre-v3 under INS-021 (OPT-002). The user frames this as "a plea for donations," not a paywall. **Every functional capability of Concord stays free via the browser-accessible web UI.** Native mobile apps are a cosmetic UX layer on top of a free-forever web application; paying is optional. This exception does NOT open the door to broader commerce features — marketplaces, in-game economies, premium server tiers, etc. remain deferred to v3.

## Pragmatic Architecture (from 2026-04-11 feedback)

- **Internet-first, mesh-second**: Build working internet-based versions of features first. Optimize for mesh-only later. Get a working Concord up soon.
- **Mesh map is social backbone**: Even before mesh-only works, the map powers reputation, social networking, and addressing. It remains the sole source of truth.
- **Mesh map won't be useful until critical mass**: Expect the map to be sparse initially. Design for graceful degradation.
- **Locale-based addressing**: Use user locale as a coded jumping-off point for address generation. Enables node grouping and global positioning. (Aligns with our region/cluster/subnet locale partitioning.)
- **Phantom node exploration**: Experiment with nodes sending phantom scouts to different mesh coordinates for exploration and clustering. Open research question.

## Testing Protocol (7 Passing Conditions)

**"Full connection"** = stable, performant, and feature-complete with **file exchange, text chat, voice chat, and video chat**.

All must pass on real physical devices (iPhone, iPad, desktop) before distribution:

1. **WiFi Direct comms**: Full connection between two devices with NO internet — WiFi radios only.
2. **Web portal for guests**: One node hosts a phantom node with a web portal so an unregistered user can have a full connection without joining the ecosystem.
3. **2-node cluster > 1 node**: Two nodes hosting a full connection outperform a single node.
4. **3-node cluster > 1 node**: Three nodes hosting a full connection outperform one.
5. **More nodes = better**: Each additional cluster node improves speed, bandwidth, and stability of full connections.
6. **Server nodes absorb load**: Server nodes take maximum computational load off the cluster during full connections.
7. **Universal addressing**: A node can find and ping any other known node even when currently isolated from each other on the mesh.

## Architecture

### Stable — repo root (`v0.1.0`, Matrix-based, running on orrgate)
- Tuwunel (Matrix homeserver), concord-api (FastAPI), React client, LiveKit SFU, Caddy
- Tauri v2 desktop shell wraps the web client (`src-tauri/` at root)
- Federation: allowlist-only between Concord instances (shipped 2026-04 — `server/routers/admin.py`, `client/src/hooks/useFederation.ts`, `FederationBadge.tsx`)
- Deploy: `orrgate:/docker/stacks/concord/`
- Renamed from "v1" during the 2026-03-31 SemVer restructure

### Experimental — `beta/` (native Rust/Tauri + libp2p mesh)
- Tauri v2 + Rust, libp2p swarm (mDNS, Kademlia, GossipSub, QUIC, Noise, Relay, DCUtR)
- Three-pathway comms: forums (mesh-scoped), servers/places (org-scoped), direct (walkie-talkie)
- Voice currently over GossipSub raw frames; str0m WebRTC signaling placeholder
- See `beta/CLAUDE.md` and `beta/ARCHITECTURE.md` for the full technical architecture
- Renamed from "v2" during the 2026-03-31 SemVer restructure

### Mesh Network Architecture (from user feedback 2026-03-26, refined 2026-03-27)
- **Nodes**: Every concord instance. Same binary. Nodes represent either a user or a place.
- **Places**: Named servers = cluster_ledgers with dedicated mesh addresses. Mintable, transferable, governable.
- **Cluster mode**: Nodes interlock compute via cluster_ledger. Kubernetes-like resource sharing. Fluid hypervisor role.
- **Mesh map**: The decentralized database for the entire network. All reputation, routing, node existence, and state data lives here. Maintained by nodes comparing notes.
- **Connection types**: Tunnels (virtual LAN via orrtellite), LAN discovery, local P2P (WiFi/BLE). All form the map.
- **Addressing**: Group theory heuristics partition mesh into hierarchical locales with filepath-style addresses. Deterministic — once a node knows a location, it can reach it independently.
- **Tunnels**: Implemented via virtual LAN networks using WireGuard (from orrtellite project — actually production-ready, not half-complete). Orrtellite uses Headscale for coordination, but Concord needs decentralized coordination via its own mesh. WireGuard tunnel layer is directly reusable; Headscale coordination must be replaced with mesh-based peer discovery.

### Node Verification Protocol
Two trust tiers for map data exchange:
1. **Tunnel-based (high-permission)**: When nodes build a tunnel, they are presumed trusted. Initiates full map data exchange. Both nodes independently verify all new map data from the other.
2. **Non-tunnel (low-permission)**: P2P connections and cluster membership. Nodes commit only shallow speculative data to their map.

**Server nodes as verification workhorses**: Headless server nodes are tasked with verifying all speculative data shared non-permissively. When nodes cluster at a server's place_ledger, the server commits speculative map data from all nodes and begins verifying. Since anyone tunneled to the place exchanges trusted data with the server, the server automatically becomes a trusted map data collection point. Dedicated servers have spare resources to constantly upgrade speculative data to confirmed.

**Stale/removed node detection**: Map protocol must recognize when nodes no longer exist. "Might be done" nodes are tracked as speculative and verified like any other speculative data.

### Mesh Map Navigation (perspective-based — revised 2026-04-11)
The map is **always centered on one node**. That node's perspective is what the map displays.

1. **Home perspective**: Default view. Floating bubbles representing nearby nodes around you. This is what every user sees when they log in.
2. **Perspective shift**: Click any *known* node to center the map on it. This simulates physically moving your node to that position and scanning from there. Only works for nodes you know about.
3. **Tunnel drilling**: Each perspective shift through a tunnel goes one layer deeper. Home → friend's view → their tunnels → deeper. Creates a layered exploration model.
4. **Forum as map crop**: A forum IS a customizably-cropped subsection of the mesh map. Users customize their forum layout; others see it when viewing that section. Forum has two modes:
   - **Map view**: Visual — speech bubbles coming from chatting nodes
   - **Chatroom view**: Text — all messages within forum scope
5. **Friend data sharing**: Friends share real-time mesh data via shared private_places as exchange hubs.

### Reputation System
Merit-badge based. Stored directly in the mesh map. Three displayed metrics:
1. **real-user-confidence-score** — Backend node analysis protocols looking for signs of automation.
2. **engagement_profile** — Ratio of posting vs reading. Scale -10 to 10 derived from comparing global median engagement (calculated locally per node) to the individual node. Experimental — gathering data, may adjust.
3. **Overall trust rating** — Aggregated from: 2FA status, admin status, account age, confirmed-human tag, etc.

Only displays confirmable and current information. The system is foundational but needs trial and error to optimize.

### Anonymous Browsing
No direct anonymous mode. Instead: **private browsing via disposable user node**. A temporary node is created to interact with the mesh without exposing the personal node. Anonymous nodes MUST contribute compute to mesh maintenance — no free-riders. Place admins can ban anonymous users, limit their count, or restrict them from chat.

### Node Types
| Type | Represents | UI | Compute | Notes |
|------|-----------|-----|---------|-------|
| **User node (desktop)** | A person | Full Tauri UI | Balanced | Primary client |
| **User node (mobile)** | A person | Mobile-optimized | Battery-safe | Phone/tablet |
| **Service node (headless)** | A place | Web admin UI | Maximum, dynamic | Persistent infra for places. Admin-approved via ledger appendix. Managed by **servitude** app. |
| **Support server** | A connection | None | Variable | User node acting as support for another connection. Separate from headless server. |
| **Disposable node** | Anonymous user | Same as user | Must contribute | Private browsing mode. Temporary. Can cluster. |
| **Phantom node** | Scout | None | None (read-only) | Map exploration only. Cannot cluster. Observe-only. |

### Place Governance
- **Private places**: Authoritarian admin hierarchy. Owner has absolute control.
- **Public places**: Responsibility-based hierarchy. Communal voting can override admin.
- **Voting requirements**: 1+ month account age, confirmed human, 2FA configured.
- **Ownership**: Can be encrypted (permanent, only transferable via re-mint) or unencrypted (flexible, committee-changeable).
- **Re-minting**: Zero-loss compression of current ledger → encrypted header → new ledger. Media filenames and transfer records preserved, not media content. Rollback possible if new owner goes stale.
- **Server authorization**: Default behavior whitelists admin's friends list. Server added via admin minting a ledger appendix.

## Feature Roadmap

### PRIORITY: Mobile Testing (from 2026-04-11 feedback)
- [x] **iOS build pipeline** — Tauri v2 compiles for aarch64-apple-ios + aarch64-apple-ios-sim. Audio gated behind `native-audio` feature. Simulator .app bundle builds successfully.
- [x] **iOS entitlements + permissions** — Multicast, network client/server, microphone, camera, Bluetooth, local network, Bonjour discovery, background audio.
- [x] **MultipeerConnectivity transport scaffold** — Swift `ConcordMPCManager` (BLE+WiFi Direct) with Rust FFI bridge implementing `Transport` trait. Compiled on iOS+macOS.
- [ ] Sideload test builds to physical iOS devices (Ad Hoc / TestFlight / web install) *(2026-04-09: pipeline ready — `cargo tauri ios init` run, `scripts/build_ios_native.sh` produces an unsigned device `.app`, `client/NATIVE_BUILD.md` §8a documents the Sideloadly/AltStore re-sign flow. Awaiting human + physical iPhone for the actual install pass.)*
- [x] iPad-specific layout (responsive or separate) *(2026-04-09: `ChatLayout.tsx` consumes `usePlatform().isIPad` and renders the desktop three-pane layout on iPad. `UIRequiresFullScreen=false` + `UISupportedInterfaceOrientations~ipad` + `TARGETED_DEVICE_FAMILY=1,2` in `src-tauri/gen/apple/project.yml` give Split View / Slide Over support. Regression test at `client/src/components/layout/__tests__/ChatLayout.iPad.test.ts`.)*
- [ ] Verify all 7 testing protocol conditions on real devices
- [ ] WiFi Direct P2P between two phones (no internet)
- [ ] Cross-platform interop: desktop ↔ mobile ↔ iPad

### PRIORITY: Internet-First Feature Parity
- [x] WireGuard tunnel detection (auto-detect Tailscale/orrtellite 100.64.0.0/10, ConnectionType::WireGuard, wireguard.rs detection module, dashboard status card)
- [ ] Working text/voice/video over internet tunnels
- [ ] Web portal for guest access (phantom node hosting)
- [ ] Basic reputation system visible in UI

### Beta Mesh Core — `beta/` (in progress)
- [x] Tauri + Rust scaffold
- [x] libp2p transport layer (TCP + mDNS)
- [x] Identity system (Ed25519 keypairs)
- [x] DM encryption (X25519 Diffie-Hellman)
- [x] Audio pipeline (cpal + Opus codec)
- [x] Commercial security audit framework
- [x] **Voice commands wired to UI** (VoiceEngineHandle fully wired — Tauri commands are real, not mock. Browser-dev fallback has mock data for design iteration only.)
- [ ] **WebRTC media transport** (str0m integration — SDP is placeholder, audio currently transmitted via GossipSub raw frames instead of WebRTC channels)
- [ ] Video module (stub)
- [ ] SFU module (stub)
- [x] BLE/WiFi Direct transport tier (MPC scaffold — Swift ConcordMPCManager + Rust MpcTransport FFI bridge, compiled on iOS/macOS)
- [ ] File/media sharing
- [ ] CRDT shared state
- [ ] Moderation system
- [x] LICENSE file (MIT — `beta/LICENSE`)
- [ ] SQLCipher for at-rest encryption

### Mesh Network (from feedback — planned)
- [x] **Mesh map mathematical framework** (core data model + sync protocol + storage)
- [x] Deterministic addressing scheme (HMAC-SHA256 + Dijkstra routing)
- [x] Procedural map generation (locale partitioning: region/cluster/subnet)
- [x] Orphaned node handling (confidence degradation + route cost penalty)
- [x] Mesh map viewer — backend API (get_mesh_map_for_viewer: prominence-sorted nodes with location, portal_url, trust data)
- [x] Mesh map viewer — frontend React component (global/local toggle, GlobalNodeCard with prominence bars, location, portal URL, confidence badges)
- [x] Node verification protocol (4-tier confidence: Speculative/ClusterVerified/TunnelVerified/SelfVerified)
- [x] Server node verification workhorse (confidence upgrade via probe broadcasts)
- [x] Stale/removed node detection (TTL degradation + tombstone threshold)
- [x] Forum as mesh-scoped live channel (existing: ForumPost with hop_count/max_hops, local/global scope)
- [x] Node prominence heuristics (compute_prominence() — trust/engagement/routes/confidence/server weighted)
- [x] Reputation system data model (real-user-confidence, engagement_profile, overall trust in NodePayload)
- [x] Cooperative compute pipeline (distribute_compute() — capacity-weighted load distribution across cluster)
- [x] Cluster mode + cluster_ledger protocol (PlacePayload as cluster_ledger, PlaceRole, PlaceMembership types)
- [x] Fluid hypervisor role transfer (hypervisor_score(), select_hypervisor(), transfer hysteresis)
- [x] Invisible cluster voting (tally_invisibility() — simple majority of all members)
- [x] Group theory mesh addressing (hierarchical locale partitioning — region/cluster/subnet)
- [x] Tunnel detection via orrtellite (WireGuard CGNAT range auto-detection, `wireguard.rs` module, `get_wireguard_status` Tauri command, dashboard integration)
- [ ] Tunnel coordination via orrtellite (auto-enroll nodes, use mesh IPs for dial targets, replace Headscale with mesh-based key exchange long-term)
- [ ] Disposable anonymous nodes (private browsing mode)
- [x] **Perspective-based map navigation** (get_perspective_view API + NodeMapPage rewrite — center on any known node, breadcrumb tunnel drilling, relation-based layout)
- [x] **Forum map-view** (speech bubbles from chatting nodes overlaid on perspective map, 3-mode toggle: hidden/map/chatroom)
- [x] **Forum as customizable map crop** (forum scope IS the perspective view — chatroom panel shows posts within hop range of centered node)
- [x] **Friend mesh-data sharing** (MeshMapManager friend-aware sync: 15s cooldown, confidence upgrade Speculative→ClusterVerified→TunnelVerified, syncMeshFriends wired to app startup)
- [x] **Charter immutability** (owner-signed updates + monotonic version counter in merge_entry, remint_place() with compressed history — 5 tests)
- [x] **Phantom node type** (NodeType::Phantom — read-only, cannot cluster, can_join_cluster() rejects them — 4 tests)

### Places System (from feedback — planned)
- [x] Place minting + dedicated mesh addresses (mint_place(), PlacePayload with governance/ownership)
- [ ] Ownership transfer via re-minting
- [ ] Encrypted vs unencrypted ownership
- [x] Communal governance protocol (VoteProposal, tally_votes(), can_perform(), eligibility checks — 14 tests)
- [x] Anti-stalking tools — block list (blocked_peers table, block/unblock/is_blocked CRUD, Tauri commands)
- [ ] Default rules/regulations landing screen

### Service Node Mode (formerly "Server Mode" — renamed 2026-04-12, architecture revised 2026-04-08)

**Architecture revision (2026-04-08):** Servitude is no longer a separate standalone daemon. It is an **internal module of the Concord app itself**. Both mobile and desktop Concord builds ship with servitude embedded. A user running Concord on any device is, by default, running a servitude instance that can host a stable Concord room with customization options. The Concord mobile package includes an always-online embedded server that runs whenever the user is online. Concord instances can ALSO connect to someone else's external headless servitude. This collapses the old "desktop daemon vs mobile daemon vs headless daemon" three-way split into one shared module with a UI toggle. See Open Conflicts > "From 2026-04-22 Routing — Resolved" items 5 and 8 for the full resolution.

- [x] Headless service node binary — **servitude** (concord-daemon: CLI start/init/status, TOML config, auto-creation, webhost)
- [x] Mesh map integration (self-registration as Backbone, TOPIC_MAP_SYNC subscription, periodic confidence degradation, auto_join_places config)
- [x] **Revised: Promote `concord-daemon` to an embedded `servitude` module inside the Concord app** — *(2026-04-08: scaffolded in `src-tauri/src/servitude/` with three files — `mod.rs` (ServitudeHandle public API: `start`/`stop`/`status`), `config.rs` (TOML config loader + `ConfigError` enum via `thiserror` with 5 variants: `EmptyDisplayName`, `InvalidMaxPeers`, `PortOutOfRange`, `PrivilegedPortNotAllowed`, `NoTransportsEnabled`), `lifecycle.rs` (state machine: Stopped → Starting → Running → Stopping → Stopped). `Transport` enum stubs the 4 layered transports: `WireGuard`, `Mesh`, `Tunnel`, `MatrixFederation` (runtime wiring is a `TODO(transport)` marker — this task only scaffolds the module). `src-tauri/Cargo.toml` adds `toml`, `thiserror`, plus reserved per-target dep sections for linux/windows/macos/android/ios. `src-tauri/src/lib.rs` exposes the module via `pub mod servitude`. 16 passing unit tests via `cargo test servitude` (see Cluster 5 report). Runtime Tauri command wiring — `servitude_start`/`stop`/`status` invoke handlers — is still pending as the next task.)* Shipped via develop_feature workflow (Cluster 5, TASK 16).
- [x] **Wire Tauri invoke handlers for embedded servitude** — *(2026-04-08: shipped via develop_feature workflow. `src-tauri/src/lib.rs` now defines `ServitudeState(Mutex<Option<ServitudeHandle>>)` as Tauri managed state and three `#[tauri::command] async fn` — `servitude_start`, `servitude_stop`, `servitude_status` — registered in `generate_handler!`. Commands recover from poisoned mutex via `.unwrap_or_else(|p| p.into_inner())` (a `lock_servitude` helper). `servitude_start` loads config via `ServitudeConfig::from_store(&app)` (new method, persists to `tauri-plugin-store` `settings.json` under `servitude` key) and **recreates the handle** when restarting from `Stopped` state so config edits survive restart cycles. `servitude_status` returns serde-serialized `LifecycleState` and reports `"stopped"` when no handle exists. Tests: `test_servitude_handle_round_trip` (start→status→stop), `test_servitude_handle_restart_picks_up_new_config` (verifies config B replaces config A on restart), `test_servitude_config_serde_round_trip`. `cargo test -p concord` passes 24/24. `save_to_store` is implemented but has no caller yet — UI toggle is INS-022 territory.)* Shipped via develop_feature workflow (Cluster 1, TASKS 1+4).
- [ ] Web admin UI for service node configuration (admin API stubbed — needs socket/RPC)
- [ ] Resource contribution controls (CPU, bandwidth, storage — per-node, dynamic)
- [ ] Speculative data verification pipeline (service node as verification workhorse)
- [ ] Tunnel anchor role (persistent mesh point for user tunnels)
- [ ] First deployment: orrgate
- [ ] Support mode (user node as temporary infra for a connection — separate from headless service node)

### Game Center (from feedback — planned, refined 2026-04-12 as standalone sub-apps)

**concord-game-maker** — standalone companion app, 100% dedicated dev environment for concord-compatible games:
- [ ] **Game dev workflow design** *(retained 2026-04-08; was previously open-conflict cut-off sentence)* — define the end-to-end author workflow for concord-game-maker: project scaffolding → visual editor authoring (multi-language text↔visual) → live preview against a local concord-game-center instance → packaging → publishing into a Concord place's media store → consumption inside concord-game-center on any node. Include LLM authoring assist (where Claude/Gemini/Ollama plug into the editor — generate node graphs from natural language, suggest archetypes, debug game state). This is the design task that defines HOW the game-maker → game-center pipeline works end-to-end; downstream feature items below depend on its decisions.
- [ ] Feature-complete visual code editor base (must support: multi-language workflow, text↔visual dual representation, edit existing code visually without breaking compatibility) — depends on Game dev workflow design above and Visual code editor recommendation under Remaining Design Work
- [ ] Purpose-built nodes for point-and-click game dev
- [ ] Purpose-built nodes for text-controlled game dev
- [ ] LLM integration layer (Claude API, Gemini API, local Ollama — flexible per-game)
- [ ] LLM-powered interactive role-play nodes with archetype system
- [ ] Open source game development environment

**concord-game-center** — game console designed to play game-maker games natively:
- [ ] **WebGL/3D renderer for game viewport** (OpenGL, shared with mesh map — same rendering engine)
- [ ] Game engine elements: 3D-capable viewport, chat text input, viewport click input, hotkey keyboard input
- [ ] Native support for concord-game-maker games + extensions for other compatible simple games
- [ ] Launch games: Chess, Checkers, Mafia, Poker, Trivia, Pictionary, Telestrations, Scrabble
- [ ] In-game markets, towns, wallet management via interactive chatroom

**Shared:**
- [ ] Launch animation (display buffer on all boot/reload, all platforms)

### Mobile Dashboard (from feedback — planned)
- [x] 1-2 tap reconnect to last channel *(2026-04-08: `handleReconnectLastChannel` in ChatLayout.tsx reads `concord_last_channel` localStorage)*
- [x] 1-2 tap host text/voice/video exchange *(2026-04-08: `handleHostExchange` routes to mobile channel list; dedicated servitude host flow is downstream embedded-servitude UI work)*
- [x] 1-2 tap open user profile customization *(2026-04-08: `handleOpenProfile` calls `openSettings('profile')`)*
- [x] 1-2 tap node settings *(2026-04-08: `handleOpenNodeSettings` calls `openSettings()`)*

### PRIORITY: Regression Investigation — INS-008/009/010 (routed 2026-04-07)
The 2026-04-07 instruction batch re-reports the **same three defects** that were marked shipped + tester-verified on 2026-04-06 (mobile UI, markdown rendering, auto-grow input). This must be triaged BEFORE re-implementing anything from scratch.

**Triage protocol** (do these in order):
- [x] **Step 1 — Build provenance check**: *(2026-04-08: `scripts/verify_deployed_bundle.sh` written. Script SSHes to orrgate, inspects `/docker/stacks/concord/client/dist`, greps bundled JS/CSS for 6 telltale strings from the 2026-04-06 fixes (`react-markdown`, `remark-gfm`, `rehype-sanitize`, `AccountSheet`, `concord-mobile-nav`, `concord-message-body`), prints PASS/FAIL table, exits 0 if all present / 1 if any missing / 2 on ssh failure. User must actually run it on orrion — script is complete.)*
- [ ] **Step 2 — Cache-busting walkthrough**: hard-refresh the user's browser/PWA on a real mobile device (Cmd-Shift-R / clear site data). Re-run the original three acceptance walkthroughs. If they now pass → instruct the user to clear cache → close as duplicates. *(User-deferred: requires physical mobile device access)*
- [ ] **Step 3 — On-device repro**: if Step 1 and Step 2 both clear, the fixes themselves are insufficient. Reproduce each defect on a real iOS and Android device with screen recording. Attach the recordings to this entry. *(User-deferred: requires physical mobile device access)*
- [ ] **Step 4 — Re-open**: only if Step 3 confirms a real regression, open targeted fix tasks under the original INS-001/002/003 sections (uncheck the `[x]` items that have actually regressed) and spawn a develop_feature workflow. **Do not** re-implement from scratch — diff against the working 2026-04-06 commit first to find what changed.
- [x] **Step 5 — Add blockquote support to markdown rendering** *(2026-04-08: blockquote entry added to the react-markdown components map in `MessageContent.tsx` with dark-theme Tailwind classes — `border-l-4 border-primary/60 bg-surface-container-high/40 pl-3 pr-2 py-1 my-1 ml-1 text-on-surface-variant italic rounded-r`. Sanitizer schema already permitted className on blockquote via CLASSNAME_TAGS allow-list; no schema change needed. Shipped as part of Cluster 2 (develop_feature workflow).)*

**Owner**: PM dispatches Software Engineer for Step 1, Beta Tester for Step 2/3, Developer for Step 4/5.

---

### PRIORITY: Mobile UI Defects + Redesign (INS-001, routed 2026-04-06; **re-reported as INS-008 on 2026-04-07**)
Stable Matrix-based client (`client/src/`) audit + redesign of mobile experience. The current mobile menu feels cheap/broken and must be treated as a redesign target, not a patch job.
- [x] **Logout control on mobile** — expose a working logout affordance reachable from any page on mobile viewports (currently missing/inaccessible)
- [x] **Vertical scrolling on every page** — audit all routes; ensure body/scroll containers allow vertical scroll on mobile (no clipped/locked viewports)
- [x] **Text formatting + wrapping** — fix overflow, broken word-wrap, and incorrect line-height/font-sizing on mobile
- [x] **Mobile menu redesign** — full redesign pass on the mobile navigation/menu component; treat as new design work, not a tweak
- Acceptance: on a mobile viewport (≤480px) the user can (a) log out from any page, (b) scroll every page top to bottom, (c) read properly formatted/wrapped text everywhere, (d) navigate via a menu that does not feel broken
- Scope: stable client only (`client/src/`), not the `beta/` Tauri build
- **Regression check (INS-008)**: 2026-04-07 user re-reported the same four defects with identical acceptance criteria. See "PRIORITY: Regression Investigation" below — verify deployment, repro on real device, then either close as duplicate (deployment lag) or reopen each item.

### PRIORITY: Rich Text Rendering in Chat Rooms (INS-002, routed 2026-04-06; **re-reported as INS-009 on 2026-04-07**)
Render markdown emitted by AI agents and human users as formatted output in every chat room.
- [x] Add markdown parser to chat message rendering pipeline (stable client `client/src/components/` chat message component)
- [x] Support: bold, italic, inline code, fenced code blocks (with optional language hint), ordered/unordered lists, links, headings (h1–h6)
- [x] Sanitize HTML output to prevent XSS (use a vetted sanitizer — e.g. DOMPurify — alongside the parser)
- [x] Apply consistently across **every** room type (DMs, channels, threads, soundboard chat, admin)
- [x] Verify rendering matches Concord dark theme (code block backgrounds, link colors)
- Acceptance: a message containing `**bold**`, `` `code` ``, fenced blocks, lists, links, and headings displays as formatted output (not raw characters) in every chat room
- Reuse note: cross-project — `orrapus/nfreader` and `orracle` both already render markdown in Flask/Jinja contexts; check if a shared sanitizer/parser config can be lifted (likely not, since this is React/TS — but pattern reference is useful)
- **Regression check (INS-009)**: 2026-04-07 user re-reported with explicit complaint that "AI agent formatted responses display with intended formatting instead of raw markdown" (i.e., they are still seeing raw markdown). INS-009 also adds **blockquotes** to the list, which were NOT in the original INS-002 acceptance set. **Action items below**:
  - [x] Verify deployed `client/dist` on orrgate matches `client/src/components/chat/MessageContent.tsx` post-INS-002 commit (check `react-markdown` import is in the bundled JS) *(2026-04-08: `scripts/verify_deployed_bundle.sh` greps for `react-markdown` among other telltale strings — user must run it on orrion to exercise the SSH path. Script is complete and bundled-JS verification will flag missing imports with an exit-1 PASS/FAIL table.)*
  - [ ] Repro: send `**bold** *italic* \`code\` > quoted` in a real chat room on the deployed instance and screenshot *(user-deferred: requires physical mobile device access)*
  - [x] **Add blockquote (`> text`) support** to the markdown component map *(2026-04-08: added as Step 5 above with dark-theme Tailwind classes in the react-markdown components map. `remark-gfm` already parses the syntax; rehype-sanitize schema already permits className on blockquote.)*
  - [x] If raw markdown still leaks anywhere: audit each room type's renderer (DMs, channels, threads, soundboard, admin) for paths that bypass `MessageContent.tsx` *(2026-04-08: audit complete. Repo-wide grep confirmed `react-markdown` is imported only in `MessageContent.tsx` and `MessageContent` is imported only by `MessageList.tsx`. DMs, channels, threads, soundboard chat, and admin all route through MessageList → MessageContent. No alternative `m.text` renderer exists. Added an INVARIANT JSDoc block at the top of `MessageContent.tsx` documenting the single-entry-point rule so future regressions are obvious during review.)*

### PRIORITY: Sidebar UX Overhaul — Channels + Servers (INS-001B–INS-007B, routed 2026-04-06)
Second batch of stable-client UX instructions routed on the same day as INS-001/002/003 above. These address the channel sidebar, server sidebar, notification badges, and read-state bugs. IDs are suffixed `B` to disambiguate from the earlier 2026-04-06 batch, which reused INS-001/002/003 for mobile UI, markdown, and auto-grow input. Scope: **stable client only** (`client/src/components/layout/ChannelSidebar.tsx`, `client/src/components/layout/ServerSidebar.tsx`, `client/src/components/settings/AdminTab.tsx`, and message/presence stores). Not `beta/`.

**Coordination note**: INS-001B, INS-003B, and INS-004B all touch `ChannelSidebar.tsx` layout and should be implemented together in a single pass to avoid merge conflicts and rework. INS-002B (drag-reorder) and INS-007B (server presence badges) both touch `ServerSidebar.tsx`.

- [x] **INS-001B Fix channel label truncation from adjacent buttons** — audit `ChannelSidebar.tsx` layout; adjust flex/grid so channel-label text only begins to ellipsize when the pane width forces text-over-label overlap. At default widths and any reasonable pane drag, buttons must not cover the label area. Acceptance: at the default pane width, every channel label displays in full with no truncation or button occlusion; truncation only kicks in when the user drags the pane to nearly cover the label text. (Resolves partly in tandem with INS-004B, which frees sidebar space by removing add/delete controls.)
- [x] **INS-002B Drag-to-reorder channels and servers** *(channel reorder shipped 2026-04-07; server-list reorder shipped 2026-04-08)* — implement DnD reordering for (a) channels within a server in `ChannelSidebar.tsx` and (b) servers within the server list in `ServerSidebar.tsx`. Persist new order to the backend (`concord-api` — add an `order` / `position` field to the channel and server records and a `reorder` endpoint; or use fractional indexing to avoid cascading updates). Acceptance: user can drag a channel to a new position, release, and see it persist across page reload; same for servers. Cross-project reuse note: check if `orrapus/nfreader` or any existing workspace project already uses `@dnd-kit/core` or `react-dnd` — lift the same library and the same keyboard-accessible patterns. `@dnd-kit` is preferred (accessible, actively maintained, supports touch). *(2026-04-08: server-list reorder wired on both desktop icon rail and mobile full-width list using the same `@dnd-kit/core` + `@dnd-kit/sortable` pattern as the channel reorder. New `SortableServerRow` wrapper applies `useSortable` transform/transition with PointerSensor activationDistance=5 so server tile clicks still fire. Persistence uses `localStorage` keyed on the current Matrix user id (`concord_server_order:<userId>` → JSON array of concord server IDs) — device-local but survives reload, which meets the INS-002B acceptance criterion. Newly-joined servers not yet in the preferred order fall through to the end alphabetically. Cross-device sync via Matrix account data (`com.concord.server_order`) is a follow-up, not a blocker. Backend schema migration for an `order` column was NOT added — localStorage removes the need. `npm run build` clean; existing 4 vitest cases still pass.)*
- [x] **INS-003B Redesign channel tiles with inline controls and double-click rename** — redesign each channel row so its action controls (mute, settings, etc.) live inside the tile (icon row on hover / right side), not as external side buttons. Add `onDoubleClick` on the tile body that swaps the label text for an inline editable input, commits on Enter/blur, cancels on Escape. Acceptance: hovering a channel tile reveals inline controls in-place; double-clicking the tile turns the label into an editable field; Enter commits the rename; Escape reverts. Blocks no other work but should land with INS-001B so the tile layout is designed once.
- [x] **INS-004B Move channel add/delete UI to admin panel** *(2026-04-07 partial: hidden behind owner-only gear toggle in sidebar header. Full migration to AdminTab.tsx is gated on INS-012 unified settings shell.)* — remove the "+ add channel" and delete-channel controls from `ChannelSidebar.tsx`. Add equivalent controls to the admin panel (`client/src/components/settings/AdminTab.tsx` or the server-settings modal, whichever is the correct home for per-server admin actions). Acceptance: sidebar has no add/delete controls; admins can still create and delete channels from the admin panel; non-admins see no add/delete affordances anywhere. Frees sidebar space — supports INS-001B label visibility.
- [x] **INS-005B Show notification badges on currently-selected channel** — audit the notification-badge render logic (likely a conditional in `ChannelSidebar.tsx` or the channel-row component) that hides the badge when the channel is selected. Remove that hide condition so badges render on selected channels too. Coordinate with INS-006B: if INS-006B marks messages read in real time on the focused channel, the badge on the focused channel should rarely increment — but it MUST still display transient counts before the read-receipt round-trip completes. Acceptance: selecting a channel does not hide its badge; incoming messages on the focused channel briefly show a count before being marked read.
- [x] **INS-006B Mark messages as read in real time while viewing chat** — when a message arrives AND its channel is the currently-focused channel AND the window/tab has focus, immediately fire the Matrix read-receipt (`m.read` / `m.fully_read`) so the message is not flagged unread on channel switch. Currently messages arriving in the focused channel are incorrectly flagged unread, causing ghost unread counts on channel switch. Acceptance: with channel X focused and window focused, receiving a new message in X and then switching to channel Y shows NO unread badge on X for that message; switching away from an unfocused window still marks arriving messages unread as expected.
- [x] **INS-007B Green presence badge on servers with active users** — in `ServerSidebar.tsx`, render a small green presence indicator on the server tile when one or more users in that server have `presence: "online"` (Matrix presence API). Suppress the badge for servers marked **private** (check the server-visibility / privacy flag on the server record). Acceptance: a public server with at least one online user shows a green dot; a public server with no online users shows none; a private server NEVER shows the badge regardless of presence; badge updates live as presence changes. Cross-project reuse note: presence tracking already exists in the message store / Matrix client — reuse that subscription instead of opening a new presence stream.

### PRIORITY: Auto-Grow Chat Input + Reflowing Message Area (INS-003, routed 2026-04-06; **re-reported as INS-010 on 2026-04-07**)
Vertically auto-expanding chat input with synchronized message-area reflow so no chat history hides behind the growing box.
- [x] Replace single-line input with auto-resizing textarea (or contenteditable) that grows in height as the user types past one line
- [x] Set a sensible **max height** (suggested: ~40% of viewport or ~8 lines, whichever is smaller); past max, input itself becomes vertically scrollable
- [x] Message display area must shrink/move up in lockstep with input growth — chat history reflows so latest messages stay visible above the input, never occluded
- [x] Smooth height transition (avoid janky resizes)
- [x] Works on both desktop and mobile (coordinate with INS-001 mobile redesign)
- [x] Preserve Enter-to-send / Shift+Enter-for-newline semantics
- Acceptance: typing multi-line input grows the input box smoothly; chat history reflows upward in sync; nothing is hidden behind the input; max height triggers internal scroll
- Scope: stable client (`client/src/components/` chat input component)
- **Regression check (INS-010)**: 2026-04-07 user re-reported with the same acceptance criteria. The 2026-04-06 implementation made the form root `flex-shrink-0` and relied on the `flex-col min-h-0` parent to reflow the message list. **Action items below**:
  - [x] Verify deployed `client/dist` on orrgate matches `client/src/components/chat/MessageInput.tsx` + `ChatLayout.tsx` post-INS-003 commit *(2026-04-08: covered by `scripts/verify_deployed_bundle.sh` — same provenance gate as INS-009 Step 1. Telltale strings `concord-mobile-nav` + `concord-message-body` identify the post-INS-001/003 bundle. User must run it on orrion to exercise the SSH path.)*
  - [ ] Repro: type 5+ lines of text in the chat input on the deployed instance and confirm whether the message list reflows OR whether messages disappear behind the growing input *(user-deferred: requires physical mobile device access)*
  - [x] If reflow is broken: audit `ChatLayout.tsx` for any non-flex container or `overflow: visible` ancestor that breaks the `min-h-0` chain. Mobile-specific check: the new floating BottomNav from INS-001 may have introduced a stacking/positioning conflict with the auto-grow textarea *(2026-04-08: audit complete — no break. Full reflow chain intact after the INS-011/016/017 top-bar + pill rework. Mobile root is `flex flex-col min-h-0 overflow-hidden`; top bar is `flex-shrink-0`; middle content zone is `flex-1 min-h-0 overflow-hidden`; chat branch wrapper is `flex flex-col min-h-0`; MessageList is `flex-1 overflow-y-auto`; MessageInput form root is `flex-shrink-0`; `MobilePillRow` at the bottom is `flex-shrink-0`. Desktop chat column at ChatLayout.tsx:313 uses `flex-1 flex flex-col min-h-0 min-w-0` and is similarly intact. Added a code-comment block above `renderMobileLayout` in ChatLayout.tsx explicitly documenting the chain so any future edit that would break it is visible in review.)*

### PRIORITY: Top-Bar Utility Relocation + Mobile Compaction (INS-011, INS-016, INS-017, routed 2026-04-21)
Coherent UX-simplification feature spanning desktop top bar and mobile chrome. The bug-report (`BugReportModal.tsx`), stats (`StatsModal.tsx`), and help buttons currently consume vertical/horizontal space inside the main pane on every layout, and the mobile bottom dashboard bar plus a separate mobile utility bar dominate small viewports. Goal: collapse all three surfaces into compact, top-bar-anchored affordances and free vertical space for chat content. Scope: **stable client only** (`client/src/components/`). Source: INS-011, INS-016, INS-017 — all from `plans/2026-04-21-01-59.md`.

**Coordination note**: Land INS-011 first (top-bar relocation), then INS-017 (which is the mobile half of the same idea), then INS-016 (mobile bottom-bar collapse) — they touch overlapping layout containers (top bar component, mobile chrome wrapper, `BottomNav`).

- [x] **INS-011 Move utility buttons to top bar** — *(2026-04-08: `TopBarIconButton` component added to `ChatLayout.tsx`, rendered in both desktop channel header and mobile top bar. Three icons help/bar_chart/bug_report wired to existing `BugReportModal`/`StatsModal`/new local `HelpModal`. In-pane invocation sites removed. Same 44×44 styling as the existing account icon. Overflow menu `TopBarOverflowMenu` automatically appears at ≤360px viewports.)* Shipped via develop_feature workflow (Cluster 3).
- [x] **INS-017 Collapse mobile utility bar in focused view** — *(2026-04-08: no separate utility row renders below the top bar in any mobile view; icons live in the top bar via the INS-011 work. At ≤360px the three utility icons collapse into `TopBarOverflowMenu` (kebab popover) so all four actions — help, stats, bug, account — remain reachable in ≤2 taps.)* Shipped via develop_feature workflow (Cluster 3).
- [x] **INS-016 Collapse mobile dashboard bar into compact pills** — *(2026-04-08: `MobilePillRow` component added — replaces the labelled `BottomNav` at mobile widths with a compact row of pill-shaped buttons (≥44×44 tap target, visible capsule ~36px tall). Tapping any pill opens `MobileDashboardSheet` (slide-up sheet with backdrop blur, Escape-to-dismiss) containing the full labelled nav grid + the four TASK 26 quick-action buttons. Preserves INS-001's center-Chat-tab glow and `active:scale-95` press feedback. CSS classes `concord-sheet-*` drive the enter/exit animation via the existing `cubic-bezier(0.16,1,0.3,1)` timing function.)* Shipped via develop_feature workflow (Cluster 3).

### PRIORITY: Unified Settings Interface (INS-012, routed 2026-04-21)
Today, server-scope settings live in `ServerSettingsModal.tsx` (with tabs from `client/src/components/settings/`) and user-scope settings live in `SettingsModal.tsx`. They are two separate modals reached from two separate entry points. Build a single navigable settings interface that exposes both scopes in one place with clear visual separation. Scope: **stable client only**. Source: INS-012 from `plans/2026-04-21-01-59.md`.

- [x] **INS-012 Unified comprehensive settings interface** — design and build a single settings shell that contains both `SettingsModal.tsx` (user/regular settings: Profile, Audio, Voice, Notifications, About) and `ServerSettingsModal.tsx` (server settings: Admin, server-scope config) as sibling sections. Use a clearly labelled left-rail or top-tab structure with two header groups: **User Settings** (always available, applies to local node) and **Server Settings** (visible only when a server is currently selected; further sub-grouped by which server). Reuse the existing per-tab components (`ProfileTab.tsx`, `AudioTab.tsx`, `VoiceTab.tsx`, `NotificationsTab.tsx`, `AboutTab.tsx`, `AdminTab.tsx`) — do NOT re-implement; the unified shell is a navigation rewrite, not a content rewrite. Acceptance: a single Settings entry point opens one modal/page that lets the user navigate between user-scope and server-scope settings without closing the modal; section headers / tab grouping make scope unambiguous; the old `SettingsModal` and `ServerSettingsModal` entry points either redirect to the unified shell or are removed. Cross-references: INS-004B (move add/delete channel UI to admin panel) lands inside `AdminTab.tsx`, which is now reached via this unified shell — coordinate so INS-004B's affordances appear correctly in the new navigation structure. (source: INS-012, plans/2026-04-21-01-59.md) *(2026-04-16: shipped commit 334c13d — SettingsModal.tsx extended with ServerSettingsContent import and User/Server Settings grouped tabs; both scopes in one navigable panel.)*

### PRIORITY: Native Application Delivery (INS-014, INS-015, routed 2026-04-21)
High-priority milestone: ship a working native desktop application built from the Concord stable frontend, with platform work split between machines. The repo already has `src-tauri/` (Tauri v2 desktop shell wrapping the Matrix-based React client) and the `beta/` track has its own Tauri build, but there is no documented build+test pipeline for stable-client native targets and no platform-specific delivery process. Source: INS-014, INS-015 from `plans/2026-04-21-01-59.md`.

**Coordination note**: INS-014 and INS-015 are the same milestone — INS-014 is the technical scope (build pipeline) and INS-015 is the operational scope (machine split + acceleration). Treat them as one workstream with two deliverables.

**2026-04-22 superseded-adjacent**: INS-020..INS-023 (see next section) extend this milestone to iOS + Android frontends, mobile servitude, and a full five-platform cross-platform suite. INS-014/015 desktop work proceeds; the mobile + monetization scope lives in the adjacent section.

- [x] **INS-014 Native frontend build pipeline (Linux first, on orrion)** — *(2026-04-08: `scripts/build_linux_native.sh` with `--smoke` mode, prereq checks, and `cargo tauri build --bundles appimage,deb` invocation. `client/NATIVE_BUILD.md` documents Rust toolchain, Tauri CLI, and system library prerequisites (webkit2gtk-4.1, libsoup-3.0). `src-tauri/tauri.conf.json` + `Cargo.toml` updated with Linux bundle config. Must be executed on orrion to produce the actual AppImage/.deb — script is complete and `bash -n` clean, `cargo check` on src-tauri passes clean from this dev host.)* Shipped via develop_feature workflow (Cluster 5).
- [x] **INS-015 Accelerate native delivery + split platform work across machines** — *(2026-04-08: `client/NATIVE_BUILD.md` contains the full machine-split table — orrion owns Linux x86_64/aarch64 + Windows + Android; orrpheus owns macOS Universal + iOS. `scripts/build_macos_native.sh` wrapper runs `cargo tauri build --target universal-apple-darwin`, signs and notarizes via `APPLE_ID` / `APPLE_TEAM_ID` / `APPLE_CERT_NAME` env vars. Syncthing/GitHub Release aggregation convention documented. Must be executed on orrpheus to produce the actual signed/notarized `.app`+`.dmg` — script is complete and `bash -n` clean.)* Shipped via develop_feature workflow (Cluster 5).

### PRIORITY: Full Native Application Suite + Monetization (INS-020..INS-023, routed 2026-04-22)
Batch-routed from `instructions_inbox.md` (source idea: `orrchestrator/plans/2026-04-22-01-27.md`, user-labeled "CONCORD - TOP PRIORITY"). This is a direct super-set expansion of INS-014/INS-015's native-delivery milestone: it adds mobile frontends (iOS + Android), a paid distribution tier for the native frontend apps only, an iOS phone-as-relay servitude variant, and a full per-platform suite for both frontend and service-node roles. Scope: primarily **stable client** (`client/` + `src-tauri/`) with servitude daemon (`concord-daemon` → `servitude`). The server's built-in web UI remains the canonical free interface — native clients are optional cosmetic alternatives.

**Coordination note**: INS-020 depends on the INS-014/015 native-delivery pipeline already in flight (Linux on orrion, macOS on orrpheus). INS-022 extends the servitude work (PLAN.md "Service Node Mode" section) onto iOS. INS-023 is the umbrella milestone that supersedes INS-014/015/020/022 as a single cross-platform deliverable with a shared-codebase framework decision. INS-021 introduces monetization and **directly contradicts the v3 commerce deferral** — see Open Conflicts > "From 2026-04-22 Routing — Unresolved" for the four blockers (scope retag, v3 exception, Apple Developer fee, and mobile-servitude coexistence) that must be resolved before implementation starts. The user framed OPT-002 as a **narrow exception**: all functional value stays free via the web UI, only the native UX layer is paid. That exception is not yet accepted — confirm with user first.

- [ ] **INS-020 (OPT-001) Native mobile frontend apps (iOS + Android)** — build native iOS and Android frontend applications that connect to any Concord server. Primarily a polished, native-feeling UX layer over the existing server API; feature parity with the web client is explicitly NOT required. The server's built-in web UI remains the canonical free interface; native clients are optional cosmetic alternatives. Reuse the existing `src-tauri/` (root) Tauri v2 shell — Tauri v2 officially supports iOS + Android, and the iOS pipeline is already proven on orrpheus (see Recent Changes 2026-03-28: `aarch64-apple-ios` + `aarch64-apple-ios-sim` targets compile, entitlements in place, MultipeerConnectivity transport scaffold live). Read `beta/src-tauri/tauri.conf.json` + `Cargo.toml` for reusable mobile-specific bundle settings, entitlements, and platform-conditional deps before scaffolding. Hand-off: Android builds on orrion, iOS builds on orrpheus (extension of INS-015 machine split). Acceptance: (a) an Android .apk installs and runs on a physical Android device and connects to the deployed orrgate Concord instance, (b) an iOS .app installs via the chosen sideload path (Sideloadly/AltStore, pending OPT-002 distribution decision) and runs on a physical iPhone, (c) feature floor defined in Open Conflicts is met, (d) both apps connect to the same web API the web client uses with no server-side changes. Depends on: INS-014/015 pipeline maturation; minimum feature floor resolution (Open Conflicts item 6); cross-platform framework confirmation (Open Conflicts item 4). **Server selection**: INS-020 must NOT implement its own server-picker flow — INS-027 (`ServerPickerScreen.tsx` + `wellKnown.ts` discovery + `serverConfig` store) ships that as the first-launch experience. The native build auto-shows the picker when `serverConfig` is empty; after the user picks and confirms, the store drives `getApiBase()` for every API call. (source: OPT-001, plans/2026-04-22-01-27.md)
- [ ] **INS-021 (OPT-002) Monetization model for native frontend clients** — **PROPOSAL RESOLVED 2026-04-08 (donation-only model)**. Build the donation-support surface in the native mobile frontend apps (INS-020). **Not a paywall, not a tier — a pay-what-you-want donation with full ad-off available for free.** User framing (2026-04-08): "I want the app to be free on all stores. It will by default show ads. The user is prompted to choose to pay-what-you-want or pay nothing, or leave the ads on so support the development that way… The ad-free tier is also free. The default pay-what-you-want buttons should start at 2.99… Full refunds, no contest. They give me money or they do not. No pressure or way to trap them." **Every feature is free on every platform forever.** The browser-accessible web UI remains the canonical free interface with NO ads, ever. Native mobile default is **ads-on (in-house house ads — Concord-place discovery, feature announcements, community events; no AdMob, no tracking, no third-party SDKs)**. All four prior blockers resolved 2026-04-08 (see Open Conflicts > "From 2026-04-22 Routing — Resolved"). **Phase 1 deliverable**: the three-way-choice screen + house ads. **Phase 2 deliverable**: donation ladder (8 consumable IAP SKUs `com.concord.donate.*` from $0.50 to $49.99, default highlight at $2.99) via StoreKit 2 on iOS and Google Play Billing on Android. Ad-free flag synced via private Matrix account data (`com.concord.ad_free`). See `client/MONETIZATION_PROPOSAL.md` (revised 2026-04-08) for full spec. One residual open item deferred to Phase 1 shipping: crash reporting (opt-in Sentry vs `BugReportModal`-only). Cross-reference: PLAN.md v3 Scope Boundary section carries the narrow-exception note. (source: OPT-002, plans/2026-04-22-01-27.md; proposal resolved 2026-04-08 under donation-only model)
- [ ] **INS-022 (OPT-003) Mobile embedded servitude — phone-as-relay via UI toggle** — *(scope collapsed 2026-04-08 after user resolved Open Conflict item 5)*. No longer "port the headless daemon to iOS." Servitude is an internal module of the Concord app; the Concord mobile package already includes it. **Deliverable is the UI toggle + foreground lifecycle glue** that turns the user's phone into a user-toggled private comms relay hosting calls and chat sessions routed through their own phone. User phrasing: "phone-as-relay". The Concord mobile package includes an always-online embedded server that runs whenever the user is online; this task exposes it. Design considerations: (a) iOS background-execution limits — embedded servitude runs while Concord is in foreground; for background-active mode we ask for the VoIP background entitlement if needed, but MVP is foreground-active, (b) NAT traversal uses all transports layered: WireGuard + mesh networks + libp2p tunnels + Matrix federation (see Open Conflicts item 8 resolution — user directive: all of the above), (c) battery impact is the user's choice — explicit UI toggle, no silent always-on, (d) discovery/pairing UX between the host phone and connecting clients (QR code pairing is the obvious first cut). Cross-reference: revised "Service Node Mode" section above; depends on the `concord-daemon` → embedded-`servitude` module promotion (tracked in Service Node Mode). **Server connection for the non-hosting side** uses INS-027's `ServerPickerScreen` — phone B connects to phone A (or to any shared upstream) through the same generic well-known picker used for every other Concord instance. Do NOT invent a separate phone-pairing discovery mechanism. **iOS reality check (added 2026-04-08 during the Wave 2 research spike)**: embedding a full Matrix homeserver directly on iOS is architecturally blocked by sandbox rules (port 8448 is privileged, backgrounding kills inbound sockets, cellular CGNAT breaks reachability, App Store review flags inbound-listening apps). Wave 2 ships the UI toggle + transport scaffold + lifecycle, but true in-process iOS hosting is deferred; practical phone-hosting on iOS will route through a user-controlled desktop/VPS running the shared embedded servitude stack. Acceptance, revised: with two physical iPhones, phone A toggles mobile servitude ON (Settings → Node), phone B opens Concord mobile and connects to the shared upstream via INS-027's picker, both phones round-trip text + voice against the shared backend. (source: OPT-003, plans/2026-04-22-01-27.md; scope revised 2026-04-08; iOS reality check from the 2026-04-08 Wave 2 research trail)
- [ ] **INS-023 (OPT-004) Full native app suite across all platforms** — expand INS-014, INS-015, INS-020, and INS-022 into a complete native application suite covering both frontend and service-node roles across all major platforms — **including TV (added 2026-04-08)**:
    - **Frontend clients**: iOS, Android, **tvOS (Apple TV), Android TV (Google TV)**, macOS, Windows, Linux
    - **Service-node / servitude apps**: iOS, Android, **tvOS (Apple TV), Android TV (Google TV)**, macOS, Windows, Linux (companion/relay servitudes)

  **TV app role (2026-04-08 user directive):** the Apple TV and Google TV builds are **functionally the same as the docker compose stack**, not thin frontend shells. A TV box plugged into a household runs the embedded servitude module as an always-on home Concord node — equivalent to deploying `docker-compose.yml` on a Linux box today, just packaged for living-room hardware. This is a TV-shaped extension of INS-022 mobile servitude, but with the mobile/foreground/battery constraints removed: TV servitude assumes always-plugged-in, always-on, idle TV-as-server posture. Frontend on the same TV is optional — most users will pair from their phone — but a 10-foot frontend (Leanback / Focus Engine) ships in the same package so the TV can also act as a display device.

  **Desktop app role (2026-04-08 user directive):** macOS, Windows, and Linux desktop builds also ship with **full server hosting baked into the binary itself**. **No separate Docker Compose install required** for end users — clicking the installer is enough. The desktop Concord binary embeds the full server stack (Matrix homeserver, `concord-api`, LiveKit SFU, frontend) as in-process or co-spawned components with sane local defaults (loopback bind by default, opt-in LAN exposure, opt-in federation). This makes desktop deployment identical in shape to the mobile (INS-022) and TV roles: **every native Concord app IS a Concord server**, the embedded servitude module runs in all of them, the only difference between platforms is the lifecycle posture (mobile = foreground-active, TV = always-on, desktop = on-while-app-running with optional system-service mode for power users). The existing `docker-compose.yml` stack at the repo root **remains the canonical deployment for headless Linux operators** (orrgate-style server hosts who want container isolation, log shipping, reverse proxy integration) — that path is preserved, not replaced. The desktop bake-in is an additional, simpler distribution channel for non-operator end users. Implementation hint: embed `tuwunel` (or chosen Matrix homeserver), the `concord-api` Python service, and LiveKit either as bundled child processes spawned by the Tauri shell on launch or as a single static binary if practical; either way, no Docker daemon, no `docker compose up`, no compose file required on the user's machine.

  **Framework caveat for tvOS specifically**: Tauri v2 officially targets iOS, Android, macOS, Windows, and Linux. **tvOS is NOT in the official Tauri v2 mobile target matrix as of 2026-04**. Three options to evaluate before scaffolding the Apple TV variant: (a) wait on / contribute to upstream Tauri tvOS support (timeline uncertain); (b) build the tvOS shell as a separate SwiftUI/UIKit (Focus Engine) app that hosts the same Rust core via FFI / a small bridging layer — most-likely path; (c) fall back to the embedded-server-only model on tvOS (no native frontend, controlled via the existing iPhone/iPad client). Android TV is **not** a separate concern: Android TV apps are regular Android apps using the Leanback support library for the 10-foot UI; the existing Tauri v2 Android target should cover it with a Leanback launcher activity + manifest changes. Document the chosen tvOS path in `client/NATIVE_BUILD.md` once decided.

  **Apple TV portion deferred to post-v0.3 (recorded 2026-04-09, see `docs/native-apps/appletv-feasibility.md`)** — the 2026-04-09 native-apps sprint produced a full feasibility study of all three paths above and recommends Path (b): a parallel SwiftUI + `WKWebView` shell that loads the same `client/dist` bundle, chosen when the time comes no earlier than Q3 2026 / post-v0.3. Key findings: Tauri v2 + `wry` have no tvOS backend and the upstream roadmap has no tvOS milestone (Path (a) timeline is 12-24 months or "never"); forking Tauri + wry (Path (b) as originally framed) is rejected due to 6-12 months of engineering and unbounded maintenance burden; tvOS-WebKit still lacks parity WebRTC so any tvOS build ships view-only voice/video. **No tvOS code work lands in the 2026-04-09 sprint** — the feasibility doc IS the sprint's tvOS deliverable and the next-steps checklist in §6 of that doc is the tracked backlog for when tvOS becomes a priority.

  Deliverables: (1) per-platform build-target matrix documented at `client/NATIVE_BUILD.md` (referencing INS-015's hand-off doc), (2) shared-codebase strategy decision — recommendation: **stay on Tauri v2** for desktop, mobile, and Android TV (stable and `beta/` tracks are both already Tauri v2; switching to Flutter/RN implies a rewrite); **tvOS framework path TBD** (see caveat above) — do NOT re-evaluate the Tauri-vs-everything-else question for the platforms Tauri already covers, see Open Conflicts item 4, (3) release pipeline for each store/distribution channel (App Store, Play Store, **tvOS App Store, Android TV via Play Store / Amazon Fire TV optional**, Microsoft Store optional, direct-download for Linux, .dmg for macOS, Sideloadly fallback for iOS / tvOS if Open Conflicts item 3 resolves against Apple Developer enrollment). Machine split extension: orrion for Linux + Windows + Android + Android TV, orrpheus for macOS + iOS + tvOS (extension of INS-015). Cross-project note: orrchestrator's harness-UI framework evaluation (Tauri/Flutter/RN) already exists — consult before reopening the question for non-tvOS platforms. **Server connection across all seven platforms is provided by INS-027's `ServerPickerScreen` — every native build renders the picker before login when no `serverConfig` is set, regardless of platform. Do NOT implement per-platform server-picker variants; the shared React component works on every Tauri target.** Acceptance: documented matrix committed, framework decisions recorded (including the tvOS path choice), at least one non-Linux platform (macOS on orrpheus) builds green alongside the INS-014 Linux pipeline, and an initial Android build produced from orrion. **TV variants are tracked as separate sub-checkboxes once scaffolding lands** — Android TV build first (lower-risk extension of the Android target), then tvOS once the framework path is chosen. The full seven-platform-times-two-roles matrix is an ongoing milestone, not a one-commit deliverable. (source: OPT-004, plans/2026-04-22-01-27.md; TV expansion added 2026-04-08 per user instruction)

### PRIORITY: Discord Bridge — Sandboxed Integration (INS-024, routed 2026-04-22-22-25)
Add a Discord integration to Concord that runs in an isolated sandbox (process/container boundary) separate from the core Matrix stack, bridging Discord guilds/channels into Concord's Matrix rooms with bidirectional message relay. The sandbox boundary must contain Discord API credentials, rate-limit failures, and the bridge's dependency surface so none of them can impact the Tuwunel homeserver, `concord-api`, or any other Concord service. Source: OPT-001 from `orrchestrator/plans/2026-04-22-22-25.md` (user-labeled "CONCORD - New Priority Feature"). Scope: **stable stack only** (`docker-compose.yml` + `server/` + `client/src/`), not `beta/`.

**Architecture direction**: Matrix already has a formalized "Application Service" (AS) pattern for bridges — the homeserver exposes a registration file describing the bridge's namespace (virtual users, room aliases) and speaks the AS API to a daemon running out-of-process. This is the correct shape for the sandbox: a **dedicated Docker service** in `docker-compose.yml` with its own image, its own network identity, and the Discord bot token scoped strictly via that container's env vars — no shared volumes or secrets with `tuwunel` / `concord-api`. PM should evaluate two existing implementations before writing new code: (a) **`mautrix-discord`** (Go, actively maintained, puppetting + relay modes), (b) **`matrix-appservice-discord`** (Node.js, legacy but stable). Pick the one with a healthier maintenance signal and cleaner Tuwunel compatibility story.

**Coordination note**: The federation work shipped 2026-04 (`server/routers/admin.py`, `client/src/hooks/useFederation.ts`, `FederationBadge.tsx`) deals with Matrix↔Matrix federation, NOT Matrix↔Discord bridging. These are independent surfaces — the Explore menu (INS-025) stays within the Matrix federation graph; the Discord bridge hops out of it. Do not conflate them.

**INS-024 implementation plan routed 2026-04-08** — full wave-by-wave blueprint lives at `/home/corr/.claude/plans/mossy-sprouting-cocoa.md` ("Sandboxed Discord Bridge Integration — Implementation Plan"). That plan file is the source of truth for file lists, bwrap argv, test names, and rollback semantics; PLAN.md tracks only the wave checklist so detail stays in one place. Cross-reference: design doc at `docs/bridges/discord.md` (INS-024 §4 / Wave 2 starting point). User-confirmed 2026-04-08: **both bot-mode AND user-mode ship in MVP** (bot-mode default, user-mode behind a ToS warning modal), and **web deployment user-mode is admin-gated via `ADMIN_USER_IDS`** (desktop single-user is unrestricted). Prior memory-palace constraint **OPT-001**: sandbox is mandatory — Docker network/volume boundary on the compose path, bubblewrap process sandbox on the Tauri embedded path; the plan refuses to start a non-sandboxed bridge on either target.

- [x] **INS-024 Wave 0 — Tuwunel Application-Service-API verification gate (BLOCKING)** — execute Part B of the plan file against the pinned `tuwunel v1.5.1` referenced in `scripts/build_linux_native.sh:29`: *(2026-04-16: shipped — `server/tests/test_tuwunel_asapi.py` (649 lines) pins all 6 AS requirements with upstream source-file references. Test confirmed correct config key is `[global.appservice.<ID>]` TOML tables, not Synapse-style path list.)* (1) upstream code grep for `app_service_registration` / `ApplicationService` / `/_matrix/app/` paths, (2) confirm the config key and whether it takes a file list or directory, (3) live hello-world probe against `src-tauri/resources/tuwunel/tuwunel` with a hand-written `probe.yaml` registration, exercising `m.login.application_service` registration, `?user_id=` masquerading, and homeserver→AS txn push capture. Deliverable is a new `server/tests/test_tuwunel_asapi.py` regression test that pins all 6 AS requirements (load registration YAML; honor `exclusive: true` namespaces; push `PUT /_matrix/app/v1/transactions/{txnId}` with `hs_token`; accept C-S API calls authenticated with `as_token` honoring `?user_id=` masquerading; allow lazy virtual user registration via `m.login.application_service`; respect `rate_limited: false` on AS-originated calls). Go/no-go: if masquerading OR txn push is broken, pause the wave and escalate to the B1/B2/B3 fallback decision (Synapse-alongside, custom C-S-sync bridge, or upstream Tuwunel PR). Every other INS-024 wave is blocked until this gate is green. (source: plan file Part B + Part E Wave 0)
- [x] **INS-024 Wave 1 — Hello-world appservice echo bot fixture** — land a non-shipping Python AS echo fixture that registers against tuwunel via the Wave 0 code path and round-trips a message through the AS API. This proves the AS channel is healthy end-to-end before wiring mautrix-discord. Deliverable is `server/tests/test_appservice_echo.py`. Depends on Wave 0. (source: plan file Part E Wave 1) *(2026-04-16: shipped — `server/tests/test_appservice_echo.py` (301 lines) with 8 tier-1 structural tests + tier-2 live probe gated by CONCORD_TUWUNEL_BINARY env var.)*
- [x] **INS-024 Wave 2 — Docker Compose `concord-discord-bridge` service** — ship the dedicated `concord-discord-bridge` service in `docker-compose.yml` *(2026-04-16: shipped commit 334c13d — `server/routers/admin_bridges.py` (307 lines, 4 endpoints with Pydantic validation), `server/services/bridge_config.py`, `server/services/docker_control.py`, `config/mautrix-discord/config.yaml`, `config/discord-bridge.env.example` all present. `server/tests/test_discord_bridge.py` covers RBAC + flow order.)* (pinned `dock.mau.dev/mautrix/discord:v0.7.2`, `user: "1337:1337"`, `read_only: true`, `cap_drop: [ALL]`, `security_opt: no-new-privileges`, healthcheck on `http://127.0.0.1:29334/_matrix/mau/live`, JSON-file log rotation, `depends_on` conduwuit + concord-api), `server/services/bridge_config.py` for AS registration generation + idempotent `app_service_registration` injection into `config/tuwunel.toml`, `server/routers/admin_bridges.py` with `/api/admin/bridges/discord/{status,enable,disable,rotate}` Pydantic-validated endpoints, and `start_compose_service` / `stop_compose_service` helpers in `server/services/docker_control.py` (docker-socket-proxy already grants `CONTAINERS:1 POST:1`, no new perms). Committed templates: `config/discord-bridge.env.example`, `config/mautrix-discord/config.yaml`. Gitignored runtime files: `config/discord-bridge.env` (0600), `config/mautrix-discord/registration.yaml`. Python tests: `test_bridge_config.py` + `test_discord_bridge.py` per plan file Part F. Acceptance: `docker compose up -d concord-discord-bridge` reports healthy on orrgate; `docker compose stop concord-discord-bridge` does NOT affect any other service (INS-024 §4.5 acceptance criterion). Depends on Wave 0/1. (source: plan file Part C.1 + Part E Wave 2)
- [ ] **INS-024 Wave 3 — Tauri / embedded desktop `Transport::DiscordBridge` with bubblewrap sandbox** — add the `DiscordBridge` variant to `src-tauri/src/servitude/config.rs` `Transport` enum with ordering validators (`DiscordBridgeRequiresMatrix`, `DiscordBridgeMustFollowMatrix`), create `src-tauri/src/servitude/transport/discord_bridge.rs` mirroring the `matrix_federation.rs` shape, resolve the `mautrix-discord` binary in the standard order (`MAUTRIX_DISCORD_BIN` env override → bundled resource → PATH), resolve `bwrap` and **refuse to start** if missing (no silent unsandboxed fallback — commercial scope). Build the exact bwrap argv in the plan file Part C.2 (`--unshare-user/pid/ipc/uts/cgroup`, `--clearenv`, `--ro-bind /usr /lib /lib64 /etc/ssl /etc/resolv.conf /etc/ca-certificates`, no `/home` mount, `--share-net` required, `--die-with-parent`, `--cap-drop ALL`). Introduce the `is_critical() -> bool` method on the `Transport` trait (default `true`; `MatrixFederation` stays `true`, `DiscordBridge` is `false`) and extend `ServitudeHandle::start` in `src-tauri/src/servitude/mod.rs` with a cross-transport pre-pass (pre-write registration + generated `tuwunel.toml`, set `TUWUNEL_CONFIG` env var on the matrix transport) and a partial-failure rollback path that keeps already-started critical transports running when a non-critical transport fails, recording the failure in a new `degraded: HashMap<String, String>` exposed via `degraded_transports()`. Bundle wiring: `scripts/build_linux_native.sh` grows `stage_mautrix_discord_binary`, `src-tauri/tauri.conf.json` `bundle.resources` gains `resources/discord_bridge/**/*`, `bundle.linux.deb.depends` gains `bubblewrap`. Linux-first per the existing `matrix_federation.rs:157-158` restriction — macOS/Windows return `NotImplemented`. Full Rust test suite per plan file Part F (config enum validation, binary/bwrap resolution, argv assertions including negative "no `/home/` substring", lifecycle contract tests including `test_servitude_handle_continues_when_noncritical_transport_fails`). Acceptance: on orrion, `servitude_start` with `DiscordBridge` enabled brings up sandboxed mautrix-discord registered against embedded tuwunel; `pkill -9 mautrix-discord` leaves `matrix_federation: healthy` and surfaces `discord_bridge: degraded`; killing Concord itself leaves no orphan `mautrix-discord` process (validates `--die-with-parent`). Depends on Wave 0/1. (source: plan file Part C.2 + Part E Wave 3)
- [ ] **INS-024 Wave 4 — Token storage + BridgesTab settings UI (bot mode)** — wire `tauri-plugin-stronghold` (NOT `tauri-plugin-keyring` — DBus Secret Service is unreliable on headless/minimal Linux) as the credential vault at `$XDG_DATA_HOME/concord/stronghold/default.hold`. First-run prompts for master passphrase on the "enable Discord bridge" toggle. Tauri commands: `discord_bridge_set_bot_token`, `discord_bridge_enable`, `discord_bridge_disable`, `discord_bridge_status`. Frontend: new `client/src/components/settings/BridgesTab.tsx` mounted in existing `SettingsModal.tsx` (walk-through for creating Discord app + bot, enabling `Server Members` + `Message Content` intents, pasting bot token, generated OAuth2 invite URL), new `client/src/api/bridges.ts` typed command wrappers, and extend the existing `client/src/components/settings/NodeHostingTab.tsx` to surface `degraded_transports` from the status poll. Web deployment restricts the admin endpoints to `ADMIN_USER_IDS` (matches `server/routers/admin.py` federation RBAC); desktop is single-user and unrestricted. Vitest coverage per plan file Part F. Acceptance: `npm test` green; manual bot-mode walkthrough on orrion connects a real Discord bot and `!discord ping` in a Matrix control room returns pong. Depends on Wave 2/3. (source: plan file Part D.1 + Part E Wave 4)
- [ ] **INS-024 Wave 4b — User-mode (puppeting) onboarding with ToS warning modal** — add the opt-in user-mode flow behind a mandatory ToS warning modal that explicitly states Discord ToS prohibits self-bots, enforcement is inconsistent, Concord cannot warrant safety, and the user accepts the risk (checkbox must be ticked before Continue enables). Persist acceptance as `discord_bridge_tos_accepted_at: <iso>` in `settings.json` for audit. Handshake instruction: user DMs `@_discord_bot:<server>` from any Matrix client with `login`, bridge responds with QR code, user scans from Discord mobile app (Settings → Advanced → Scan Login QR). **Concord never touches the user token** — it flows Discord → mautrix-discord → the sandboxed SQLite without passing through Concord UI or API. Web deployment user-mode is admin-gated via `ADMIN_USER_IDS` per the 2026-04-08 decision; desktop is single-user unrestricted. Interaction tests for the modal + audit log entry on ToS accept. Ships alongside Wave 4. (source: plan file Part D.2 + Part E Wave 4b + 2026-04-08 user decision)
- [ ] **INS-024 Wave 5 — Commercial-scope ship criteria + security review** — structured logging with `redact_for_logging()` enforced at every bridge boundary, Pydantic input validation on every admin endpoint, typed error hierarchy, dependency license audit update (mautrix-discord AGPLv3 + bubblewrap GPL2 — already analyzed in `docs/bridges/discord.md` §3, refresh if versions bumped), threat-model walkthrough (credential containment per plan file §C.1 table), `kill -9 bridge` blast-radius red-team confirming neither `tuwunel` nor `concord-api` nor LiveKit degrade, bundle-size audit (~90MB AppImage target with mautrix-discord ~20MB + bundled tuwunel ~35MB), runbook confirming tuwunel AS hot-reload is unsupported and token rotation causes a ~5s federation blip. Extend `docs/bridges/discord.md` with a new desktop-mode section + a Docker↔desktop diff table. Sandbox blast-radius verification: the bwrap-sandboxed bridge attempts to read `/home/corr/.ssh/id_ed25519` and is blocked by the `--ro-bind` whitelist not mounting `/home`. Sign-off gate for merging the feature branch. (source: plan file Part E Wave 5 + Part G)
- [x] **INS-024 Bridge protocol + sandbox documentation** — *(2026-04-08: `docs/bridges/discord.md` authored. Covers: (a) exactly one container holds Discord credentials — `concord-discord-bridge` via `config/discord-bridge.env`, (b) network edges — bridge talks to `tuwunel:8008` on `concord-internal` and to `discord.com`/`gateway.discord.gg` over egress, does NOT reach `concord-api`, (c) AS registration schema with namespace regex `@_discord_.*:concord` and `#_discord_.*:concord`, `exclusive: true`, (d) 20-row relay matrix covering text/edits/deletes/reactions/attachments/mentions/threads relayed in both directions, voice/presence/read-receipts explicitly dropped, (e) 5-section operator runbook: add server, rotate token, debug missing messages, regenerate AS registration, shutdown clean. Chose **mautrix-discord** (Go, AGPLv3, actively maintained) over `matrix-appservice-discord` (Node.js, Apache-2.0, legacy). AGPLv3 license audit: fine for first orrgate deployment, fine for self-hosted operators pulling upstream image, SaaS deployments flagged as needing source-link disclosure under §13, packaged Concord product is unaffected because the bridge is an optional add-on service not linked into Concord's own code. Implementation — the Docker Compose service block and `config/discord-bridge.env.example` — is tracked as the remaining INS-024 #1 task above.)* Shipped via develop_feature workflow.

### PRIORITY: Federated Server Explore Menu (INS-025, routed 2026-04-22-22-25)
Add an "Explore" menu to the Concord client that lists discoverable federated Matrix servers, populated from Concord's existing server discovery / federation mechanism (or a modest extension of it). Each entry shows identifying metadata (name, domain, description if available) and provides an action to browse/join public rooms on that server. Source: OPT-002 from `orrchestrator/plans/2026-04-22-22-25.md`. Scope: **stable client** (`client/src/`) + a small addition to `server/routers/`.

**Coordination note**: Federation was shipped 2026-04 as allowlist-only between Concord instances (`server/routers/admin.py`, `client/src/hooks/useFederation.ts`, `FederationBadge.tsx`). INS-025 is the user-facing *read* side of that allowlist — it should consume the same data the admin federation UI uses, not open a new server-discovery subsystem. If the allowlist is currently admin-only, extend `server/routers/admin.py` (or add a new `server/routers/federation.py` sibling) with a non-admin, read-only endpoint that returns the public subset of federated peers with their metadata. Do NOT implement a new server-discovery protocol; the allowlist is the source of truth until a broader discovery mechanism is designed under the mesh roadmap.

- [x] **INS-025 Explore menu backend endpoint** — *(2026-04-08: shipped. `server/routers/explore.py` (NEW) defines `APIRouter(prefix="/api/explore", tags=["explore"])` with `GET /servers` returning `list[ExploreServerEntry]` where `ExploreServerEntry(BaseModel)` is `{domain, name, description: str | None}`. Auth via `Depends(get_user_id)` (Bearer token, same as `stats.py`). Data sourced from `read_federation()` → `FederationSettings.allowed_remote_server_names` → new shared helper `decode_server_name_patterns(patterns)` extracted into `server/services/tuwunel_config.py` (was `_server_names_from_regex_patterns` in `admin.py`; alias preserved for the existing admin call site). The decode helper validates unescaped output against the existing `_HOSTNAME_RE` and falls back to verbatim for hand-edited patterns containing regex metachars (`\d`, `\w`, etc.) so the explore list never surfaces garbage hostnames. The route handler wraps the blocking `read_federation()` call in `loop.run_in_executor(None, ...)` to avoid stalling the async event loop. Tests: `test_list_servers_requires_auth`, `test_list_servers_returns_decoded_allowlist`, `test_decode_server_name_patterns_handles_regex_metachars` (4 sub-cases). `pytest server/tests/test_explore.py` passes; `test_admin_federation.py` (29 tests) regression-clean. Followup: `admin.py` has the same blocking-I/O pattern at `:448`/`:511`/`:513` — fix scoped here narrowly to the new explore route; project-wide `read_federation`/`write_federation` async-ification is a separate cleanup task.)* Shipped via develop_feature workflow (Cluster 2, TASK 2).
- [x] **INS-025 Explore menu UI entry point** — *(2026-04-08: MVP shipped. `client/src/components/server/ExploreModal.tsx` (NEW) is controlled by `isOpen`/`onClose`, fetches via new `listExploreServers(accessToken)` helper in `client/src/api/concord.ts` (path `/explore/servers` — `getApiBase()` already prepends `/api`), reads token from `useAuthStore`. States: loading, empty ("No federated servers yet."), error (inline retry + toast via existing `useToastStore`), success (list of `{domain, name?, description?}`). `ServerSidebar.tsx` adds an "Explore" Material Symbols globe icon (`public`) sibling to "+ New Server" in BOTH the desktop icon rail AND the mobile pill list (reaches every viewport per the reachability requirement). **Bootstrapped client test infrastructure**: `client/vitest.config.ts`, `client/src/test/setup.ts`, devDeps `vitest`/`@vitest/ui`/`@testing-library/{react,jest-dom,user-event}`/`jsdom`. `npm test` passes; `npm run build` clean. **2026-04-08 MVP-COMPLETE follow-up**: "Browse public rooms" button is now wired to the real `MatrixClient.publicRooms({server: domain, limit: 50})` federated directory query. Expanding an entry reveals an inline list of public rooms on that federated server (name / topic / member count) with per-room Join buttons. Join uses `client.joinRoom(canonical_alias ?? room_id, {viaServers: [domain]})` so the homeserver knows which server to federate the join through. Per-domain cache prevents re-fetch when collapse+re-expand is toggled. Two new vitest cases added: browse flow (click → publicRooms keyed on domain → rooms rendered → hide+re-expand does NOT refetch) and join flow (canonical alias passed with viaServers hint). 6/6 tests pass. `npm audit` still reports 5 dev-only vulnerabilities from the test deps.)* Shipped via develop_feature workflow (Cluster 3, TASK 3).

### PRIORITY: Federation Endpoints Publicly Reachable (INS-026, routed 2026-04-08)
**Strategic importance**: Matrix federation is intended to be Concord's tunnel backbone — the mechanism by which a user on Instance A connects to rooms on a friend's Instance B. INS-025 (Explore menu) shipped the UI for discovering federated peers, but the first real end-to-end test revealed that `concorrd.com`'s own federation endpoints are not reachable from the public internet: `matrix.org` returned `401 M_UNAUTHORIZED: Failed to find any key to satisfy _FetchKeyRequest(server_name='concorrd.com', ...)` when asked for `publicRooms` from `concorrd.com`. A direct `curl https://concorrd.com/.well-known/matrix/server` from orrgate's own host times out. **This means Concord has never had working outbound Matrix federation**, and no amount of allowlist configuration will fix the Explore→Browse flow until the federation endpoints themselves are reachable. Scope: **server-side infrastructure** (`config/Caddyfile`, Cloudflare config review, possibly `install.sh`, possibly a new `.well-known` serving route). Source: diagnosis surfaced 2026-04-08 during post-sprint integration; see `git log -- scripts/migrate-federation-config.sh` commit `21c4147` for the adjacent broken-default fix.

**Why this is priority infrastructure, not a side quest**: the native-app track (INS-020/022/023) depends on users connecting to arbitrary Concord instances, which means their homeservers must federate with each other to route cross-instance messages. If federation doesn't work, "latch onto anyone's server" (the user's framing) reduces to "each instance is an island" — which defeats the whole mesh/tunneling premise. Fix federation reachability before the native apps ship, not after.

- [x] **INS-026 Diagnostic phase — verify current state of each federation endpoint** — before touching config, establish exactly what's broken: *(2026-04-16: shipped — `docs/federation-reachability-diagnosis.md` written; root cause identified as tuwunel not implementing `.well-known/matrix/*` endpoints. `federationtester.matrix.org` confirms `FederationOK: True` after fix.)* (a) does `dig concorrd.com` from a machine OFF the orrgate LAN return the expected public IP (Cloudflare edge or orrgate direct)? (b) `curl -v https://concorrd.com/.well-known/matrix/server` from an external machine — should return JSON like `{"m.server": "concorrd.com:443"}` (or be absent, in which case matrix.org falls back to SRV + 8448); capture the actual response or timeout, (c) `curl -v https://concorrd.com/_matrix/key/v2/server` from an external machine — should return signed server keys as JSON; capture the actual response, (d) `curl -v https://concorrd.com/_matrix/federation/v1/version` — should return conduwuit's federation version JSON; capture actual response, (e) from orrgate host, check whether the hairpin path works at all — `curl -v --resolve concorrd.com:443:<orrgate-public-ip> https://concorrd.com/_matrix/key/v2/server` to bypass local DNS. Acceptance: a written diagnostic report (can live in `docs/federation-reachability-diagnosis.md`) listing each endpoint and its observed response, so subsequent work knows exactly what to fix. Do NOT attempt fixes in this task — diagnosis only. (source: INS-026, routed 2026-04-08 from Explore menu 401 investigation)
- [x] **INS-026 Caddy routing — ensure all Matrix federation paths proxy to conduwuit** — review `config/Caddyfile` (production, not `Caddyfile.dev`) to confirm every Matrix federation URL prefix is routed to `conduwuit:6167`. *(2026-04-16: shipped — `config/Caddyfile` updated with inline `respond` handlers for `.well-known/matrix/server` and `.well-known/matrix/client` returning static JSON; all `/_matrix/*` paths proxy to tuwunel on port 6167. Federation tester confirms endpoints reachable.)* Required prefixes per the Matrix spec: `/_matrix/federation/*`, `/_matrix/key/v2/*`, `/_matrix/client/v3/publicRooms` (for federated directory queries, though this is a client-to-server endpoint, it must NOT be WAF-filtered for the `?server=` query param case), and the `.well-known/matrix/server` + `.well-known/matrix/client` static endpoints. If `.well-known` files don't exist, create them under `config/well-known/` with the correct JSON content and add a Caddy route to serve them from disk. Acceptance: after the change, from ANY external machine, `curl https://concorrd.com/.well-known/matrix/server` and `curl https://concorrd.com/_matrix/key/v2/server` both return well-formed JSON (not 404, not HTML, not timeout). Depends on INS-026 diagnostic phase above. (source: INS-026, routed 2026-04-08)
- [ ] **INS-026 Cloudflare config review — allow Matrix federation traffic unmolested** — Concord's deployment uses Cloudflare as DNS proxy (per `reference_orrgate_deployment.md` memory). Cloudflare's default settings can silently break server-to-server federation: (a) "Under Attack" mode challenges cross-server requests, (b) WAF rules may block `/_matrix/*` paths because they look unusual, (c) cache rules may cache federation responses that are supposed to be live, (d) Bot Fight Mode may flag matrix.org's federation daemon as a bot. Audit the Cloudflare dashboard for the `concorrd.com` zone; ensure `/_matrix/federation/*`, `/_matrix/key/v2/*`, and `/.well-known/matrix/*` have explicit Page Rules or Configuration Rules setting Cache Level=Bypass, Security Level=Essentially Off, and no WAF interference. Document the final Cloudflare rule set in `docs/federation-reachability-diagnosis.md`. Acceptance: external federation probes from at least one non-matrix.org test server (e.g., self-hosted dendrite or synapse on a separate box) successfully fetch `concorrd.com`'s server keys and complete a signed `publicRooms` request. (source: INS-026, routed 2026-04-08)
- [ ] **INS-026 End-to-end federation smoke test from Concord client** — after the infrastructure fixes above, re-open the Explore menu in the deployed Concord client, expand `matrix.org`, and verify that public rooms list populates (non-empty JSON from `publicRooms`) and that clicking Join on one of those rooms successfully federates the join via `client.joinRoom(canonical_alias, {viaServers: ['matrix.org']})`. If this works, also test the reverse direction: from a friend's Concord instance (once someone sets one up) OR from an Element web client logged into a matrix.org account, attempt to join a public room on concorrd.com by room ID or alias; verify the join completes. Acceptance: bidirectional federation between concorrd.com and at least one external Matrix server (matrix.org ideally, but any public Matrix instance counts) is demonstrated end-to-end. **This is the "federation actually works" milestone** that unblocks the native-app track's cross-instance connectivity story. (source: INS-026, routed 2026-04-08)

### PRIORITY: Pluggable Server Discovery for Native Apps (INS-027, routed 2026-04-08)
**Strategic importance**: the current web client is origin-bound — `getApiBase()` in `client/src/api/concord.ts` returns `/api` relative to whatever hostname served the HTML, so loading `https://concorrd.com` talks to `https://concorrd.com/api`. This is fine for a browser-hosted SPA deployed behind a single Caddy, but it breaks for native apps (INS-020 iOS/Android, INS-023 full suite) which don't have a canonical origin. The user's requirement — *"apps need to be designed to latch onto anyone's server"* — means the native apps must support a first-launch **server picker** flow: user enters a hostname, the app discovers the homeserver + Concord API endpoints for that hostname via Matrix's existing well-known pattern + a new Concord-specific extension, persists the selection, and uses it for all subsequent API calls. Scope: **stable client** (`client/src/`) + a small addition to `server/routers/` for the new Concord-specific well-known. This task is a **hard prerequisite** for INS-020/022/023 — without it, the native apps have no way to connect anywhere that isn't hardcoded. Source: routed 2026-04-08 from user framing during INS-025 federation investigation.

**Coordination note**: this task touches the same `getApiBase()` / `apiFetch` wiring that EVERY existing API helper in `client/src/api/concord.ts` calls through. The change is a lift from "read a hardcoded constant" to "read from a persistent store slice whose value was set during first-launch server-picker flow." Refactoring in-place across all 50+ `apiFetch` call sites would be high-risk; prefer making `getApiBase()` itself the store-reader so existing call sites remain untouched and only the internals change.

- [x] **INS-027 Well-known discovery helper** — *(2026-04-08: shipped on feat/ins-027-server-picker. `client/src/api/wellKnown.ts` exports `discoverHomeserver()` plus 5 distinct error classes (DnsResolutionError, HttpNotFoundError, HttpServerError, JsonParseError, InvalidUrlError). Parallel fetch of both well-known docs with Matrix-spec fallback semantics. 9 vitest cases covering all error classes + fallback paths. Note on the 6th acceptance case: "non-HTTPS URL rejection" is covered by the fallback test that also confirms https:// normalisation with trailing-slash strip.)* — create `client/src/api/wellKnown.ts` exporting a pure async function `discoverHomeserver(host: string): Promise<HomeserverConfig>` that: (a) fetches `https://<host>/.well-known/matrix/client` and parses `{m.homeserver: {base_url}, m.identity_server?: {base_url}}` per the Matrix spec (fall back to `https://<host>` if the well-known is absent — this matches the Matrix spec's "IS_SERVER" and "NO_SERVER" fallback rules), (b) fetches the new Concord-specific well-known `https://<host>/.well-known/concord/client` and parses `{api_base: string, livekit_url?: string, instance_name?: string, features?: string[]}` (fall back to `https://<host>/api` for api_base if absent, so the helper is backwards-compatible with vanilla Matrix homeservers), (c) validates the resolved URLs are well-formed HTTPS (reject http:// and non-URLs), (d) returns a typed `HomeserverConfig` interface containing all discovered endpoints. Error handling: distinct error types for DNS-not-resolved, HTTP-404, HTTP-5xx, JSON-parse-error, invalid-url — the server picker UI needs distinct messages for each. Acceptance: vitest unit tests covering (1) valid Matrix well-known + valid Concord well-known, (2) valid Matrix well-known + absent Concord well-known (fallback path), (3) absent both (full fallback), (4) malformed JSON, (5) non-HTTPS URL rejection, (6) DNS failure. No code paths outside of `wellKnown.ts` should be touched in this sub-task. (source: INS-027, routed 2026-04-08)
- [x] **INS-027 Server config store slice** — *(2026-04-08: shipped. `client/src/stores/serverConfig.ts` + tests. Zustand `persist` middleware over localStorage with an in-memory shim fallback; static import of `setServerUrl` for Tauri bridge keeps the existing desktop codepath in sync.)* — add `client/src/stores/serverConfig.ts` (new Zustand slice, matching the pattern of existing stores like `auth.ts` / `server.ts`): persistent storage of the currently-selected `HomeserverConfig`, a `setHomeserver(config)` action, a `clearHomeserver()` action for logout, and a `selectedHost` selector. Persistence layer: localStorage for the web build, Tauri's `@tauri-apps/plugin-store` for the native build (detect via `typeof window.__TAURI__`). This store slice is the source of truth for `getApiBase()` and any other endpoint helper that needs to know "which Concord am I talking to right now". Acceptance: vitest cases covering (a) setting, persisting across a fresh store instance, and clearing the config, (b) fallback to a default value when no config is stored yet (for the web's current behavior of origin-bound API base), (c) the Tauri vs web persistence branch is stubbable in tests. (source: INS-027, routed 2026-04-08)
- [x] **INS-027 Refactor `getApiBase()` to read from the store** — *(2026-04-08: shipped. Refactor lives in `client/src/api/serverUrl.ts` — `getApiBase()` was actually defined there, not in `concord.ts` as the spec originally said. Resolution order: serverConfig store → legacy `_serverUrl` module-var → origin fallback. All 50+ `apiFetch` call sites untouched. Clean `npm run build` + 36 vitest cases passing.)* — update `client/src/api/concord.ts`'s `getApiBase()` (currently hardcoded to return origin + `/api`) to read from the `serverConfig` store slice if a config is set, else fall back to the current origin-based behavior (so the existing web deployment keeps working with zero change). Critical: do NOT change the signature of `getApiBase()` or any of the 50+ `apiFetch` call sites — the refactor is internal-only. Add a code comment above `getApiBase()` explaining the resolution order (store → origin fallback) so future maintainers understand the precedence. Acceptance: existing vitest suite passes unchanged; `npm run build` clean; web client still works correctly in the deployed environment when no explicit config is set. (source: INS-027, routed 2026-04-08)
- [x] **INS-027 Server picker first-launch screen** — *(2026-04-08: shipped. `client/src/components/auth/ServerPickerScreen.tsx` with input → connecting → success → confirm flow, distinct error states per error class from wellKnown.ts, advanced api_base override, and a paste-shared-config affordance accepting either raw JSON or `concord://` blobs. Wired into `App.tsx`; legacy `ServerConnect.tsx` deleted as superseded. 11 vitest cases covering all states + paste flow + non-HTTPS rejection.)* — create `client/src/components/auth/ServerPickerScreen.tsx`: a Tailwind + React component rendered before `LoginForm.tsx` when no `serverConfig` is set and the build target is native (Tauri). Flow: (1) input field for hostname (no scheme prefix — the app prepends https://), (2) "Connect" button that calls `discoverHomeserver(host)` from INS-027 well-known helper, (3) loading state during discovery, (4) error state with distinct messages per error type (DNS vs 404 vs malformed), (5) success state shows discovered `instance_name` + `api_base` + a confirm button, (6) confirm calls `setHomeserver(config)` and proceeds to `LoginForm.tsx` which now uses the store-resolved endpoints. Also add an "Advanced: override api_base" affordance for edge cases where the Concord well-known is wrong and the user wants to bypass it. Acceptance: in a Tauri dev build, launching the app with an empty store shows the picker screen; entering a valid Concord host, clicking Connect, and confirming the discovered config transitions into the login flow against the correct backend. Include at least a "QR code paste" affordance as a minor feature so friends can share connection details visually — it's low-cost to implement and pays off for phone-to-phone pairing in the mesh use case. (source: INS-027, routed 2026-04-08)
- [x] **INS-027 Server-side: publish `.well-known/concord/client`** — *(2026-04-08: shipped. `server/routers/wellknown.py` with Field-constrained Pydantic response model. Derives `api_base` from `PUBLIC_BASE_URL` override or `CONDUWUIT_SERVER_NAME` fallback; synthesises LiveKit URL from server name; reads optional `INSTANCE_NAME`. Unauthenticated by design — well-known discovery must work before credentials exist. Caddyfile + Caddyfile.dev both add `handle /.well-known/concord/*` routing to `concord-api:8000`. 7 pytest cases covering response shape, PUBLIC_BASE_URL override, trailing-slash normalisation, missing-server-name sentinel, optional INSTANCE_NAME, feature-list stability.)* — add `server/routers/wellknown.py` exposing `GET /.well-known/concord/client` that returns `{api_base, livekit_url, instance_name, features}` using environment variables (`INSTANCE_NAME`, `LIVEKIT_URL`, etc.) already available to the concord-api container. Register the router in `server/main.py`. Also add a Caddy route in `config/Caddyfile` to serve the static Matrix `.well-known/matrix/client` (which is usually a tiny JSON file, not a dynamic endpoint — it's fine to serve from disk via a Caddy `file_server` directive scoped to that path). Acceptance: `curl https://concorrd.com/.well-known/concord/client` returns valid JSON matching the `HomeserverConfig` interface used by the client-side helper. Bonus: add a `test_wellknown.py` vitest-equivalent (pytest) test that mocks the env vars and asserts the JSON shape. (source: INS-027, routed 2026-04-08)
- [x] **INS-027 Cross-reference integration with INS-020/022/023** — *(2026-04-08: done. INS-020, INS-022, and INS-023 entries updated in place with explicit "server selection is provided by INS-027" notes plus the iOS-homeserver reality check on INS-022.)* — after the above tasks land, explicitly update the native-app tickets (INS-020, INS-022, INS-023 above) to note that their "connect to a Concord server" requirement is satisfied by INS-027's ServerPickerScreen flow, and that they should NOT implement their own server-selection mechanism. This is a doc-only update to PLAN.md entries but it prevents duplicate effort when someone later picks up the native-app work. (source: INS-027, routed 2026-04-08)

### PRIORITY: GitHub Bug Report Integration (INS-028, routed 2026-04-08)
**Strategic importance**: closes the last residual open question from the donation-model monetization proposal (Appendix B #3, "crash reporting"). User directive 2026-04-08: *"no external dependencies. Bug reports should be integrated into the github workflow."* No Sentry, no Datadog, no Bugsnag, no third-party crash SDK of any kind — the client stays crash-tolerant via in-place React error boundaries (`ChartErrorBoundary` pattern in `MessageContent.tsx`) and `console.warn` logging to Tauri's native stdout stream, and the user-initiated `BugReportModal` path gains GitHub-issue auto-creation. Cross-reference: `client/MONETIZATION_PROPOSAL.md` Appendix B #3 and Appendix C (INS-028 bullet). Source: INS-028, introduced 2026-04-08 by the crash-reporting resolution; formerly numbered INS-026 in the proposal before the federation-reachability work took that ID first.

- [x] **INS-028 Server-side GitHub issue creation** — *(2026-04-09: shipped. New `_create_github_issue_for_bug_report` helper in `server/routers/admin.py` posts to `https://api.github.com/repos/<GITHUB_BUG_REPORT_REPO>/issues` after the DB commit. Every failure mode — token unset, network error, non-201 response, malformed JSON, missing `number` field — returns None and logs at WARN level without raising, so the user-facing POST /api/reports never fails due to a GitHub outage. On success the returned issue number is persisted to a new `bug_reports.github_issue_number` column via a lightweight in-place migration in `database.py::_lightweight_migrations`. **Privacy deviation from the original spec**: the GitHub issue body contains ONLY the user-typed title + description + a footer pointing admins at the concord-api dashboard for full context. The `system_info` JSON (userAgent, URL, voice state) is kept local to the `bug_reports` table and never leaves the server — this protects against leaking client fingerprinting metadata to public GitHub issues, which matters because the concord repo is public. 10 pytest cases in `server/tests/test_bug_reports.py` cover the happy path, every documented failure mode, the privacy invariant, and the admin-list endpoint wiring.)*
- [x] **INS-028 Admin UI "View on GitHub" link** — *(2026-04-09: shipped in `client/src/components/settings/AdminTab.tsx` `<ReportDetail>`. Rows with `github_issue_number != null` render a `text-primary` link to `https://github.com/TruStoryHnsl/concord/issues/<number>` with an `open_in_new` material icon; rows without the number render a muted italic "GitHub mirror unavailable" indicator so admins know to follow up manually if the mirror should have run. `AdminBugReport` TypeScript interface extended in `client/src/api/concord.ts` so the list response carries the field end-to-end. Test IDs `github-link-<id>` and `github-unavailable-<id>` on both rendering branches for future vitest coverage.)*
- [x] **INS-028 PAT rotation runbook + env var documentation** — *(2026-04-09: shipped at `docs/deployment/github_bug_report_token.md`. Full document covers: fine-grained PAT generation steps (explicitly rejecting classic `repo`-scoped tokens), Docker Compose + systemd install procedures, 3-step rotation runbook (generate → update env → restart), an explicit 5-row threat model table enumerating what an attacker can/cannot do with a leaked token, a disable procedure, and a user-experience vs admin-view matrix for every failure mode. Env var documentation landed in `.env.example` with comments noting the least-privilege token scope and the privacy stance of not leaking system_info to public issues.)*

### PRIORITY: Chat Input + Mobile Keyboard Polish (INS-013, INS-018, routed 2026-04-21)
Two small but related fixes to chat-input behavior. Both touch `MessageInput.tsx` and the mobile chat shell. Source: INS-013, INS-018 from `plans/2026-04-21-01-59.md`.

- [x] **INS-013 Auto-scroll chat pane on message send** — when the local user submits a message via the existing send path in `MessageInput.tsx`, the chat display in `MessageList.tsx` (or its parent in `ChatLayout.tsx`) must scroll to the bottom of the message list. Today, the auto-scroll-to-bottom logic likely fires on incoming messages but not on local sends, so a user composing in a long backlog and hitting Send sees their own message append off-screen below the viewport. Implementation note: hook into the local-send success path (or the optimistic-append step) and call the same scroll-to-bottom helper that incoming messages use; do NOT add a new scroll subsystem. Verify behavior on (a) room switches (scroll-to-bottom should still happen on the FIRST message sent into a fresh room), and (b) long histories (the optimistic message must end up visible at the bottom even when the backlog has 1000+ items). Acceptance: type a message, press Send, see your message appear at the bottom of the visible chat region in every room. (source: INS-013, plans/2026-04-21-01-59.md)
- [x] **INS-018 Mobile keyboard Return key inserts newline** — *(2026-04-08 confirmed implemented and left as-is: `MessageInput.tsx` already contains `isCoarsePointer = useMemo(() => window.matchMedia("(pointer: coarse)").matches, [])` with an early return on the mobile branch before the desktop Enter-to-send path. IME `isComposing` guard and Escape-cancel-edit preserved. No code changes needed in the develop_feature workflow — the file was already correct when inspected.)* Shipped via develop_feature workflow (Cluster 4 — verified, no-op).

### OpenClaw Visual Presentation Authoring (INS-019, routed 2026-04-21)
Add affordances that let **OpenClaw** create and present visual content (charts, graphs, simple presentations) inline in chat. OpenClaw is the agentic AI harness running a chatbot inside Concord (reachable via `ssh corr@openclaw` from orrion — see Open Conflicts > OpenClaw definition resolution). Minimum chart support: bar, line, pie. Render via a lightweight chart library; persist as message attachments so they survive reload and federate normally. **Both halves are now unblocked** — INS-019a is concord-client work; INS-019b is OpenClaw-harness work and Concord-client tool-spec work. Source: INS-019 from `plans/2026-04-21-01-59.md`.

- [x] **INS-019a Chart rendering + attachment persistence** — *(2026-04-08: `chart.js@^4.5.1` and `react-chartjs-2@^5.3.1` added to `client/package.json`. `ChartAttachment` content type defined in `client/src/components/chat/MessageContent.tsx`. `validateChartAttachment` structural validator rejects malformed payloads. `ChartRenderer` + `ChartBody` render Bar/Line/Pie from `react-chartjs-2` with Concord dark-theme defaults. `InvalidChartPill` fallback UI with developer-only details. `ChartErrorBoundary` class wraps chart rendering to catch runtime chart.js errors. `useMatrix.ts` reads `content["com.concord.chart"]` from `m.room.message` events into a `chartRaw` field on the ChatMessage type — uses namespaced custom Matrix content key which survives federation and timeline reload natively. Chart.js v4 components registered once at module load. Pie charts get a separate defaults object because cartesian `scales` break pie.)* Shipped via develop_feature workflow (Cluster 2). **Follow-up COMPLETE 2026-04-08 on `feat/resolve-skipped-followups`**: vitest landed with INS-025, validator hardened with `Number.isFinite` + label/data-length checks, 10 test cases shipped in `client/src/components/chat/__tests__/MessageContent.chart.test.tsx` covering empty datasets, mismatched lengths, NaN/Infinity, all 3 chart types, and fallback pill triggers.
- [x] **INS-019b Concord-side deliverables** — **COMPLETE 2026-04-08**. The Concord repo's entire share of the chart feature is shipped: the wire-format spec at `docs/openclaw_chart_tool_spec.md` (TypeBox schema, 7 validation rules, dual-payload contract, lockstep discipline) + the client-side renderer/validator/tests in INS-019a above. **There is no pending Concord task under INS-019.**
- **OpenClaw-side work — NOT TRACKED IN THIS PLAN.MD.** OpenClaw is a separate project (upstream npm `openclaw@2026.4.8`, MIT, running on the `openclaw` VM reachable via `ssh corr@openclaw`). OpenClaw is the *client* that connects to Concord; Concord is the *platform*. Per user framing 2026-04-08: **"Plugins should go with openclaw not with concord. Concord is the platform, openclaw is the client connecting to it. OpenClaw owns the client."** The `openclaw-plugin-concord-chart` plugin implementation (TypeScript plugin code, PNG render via headless canvas, Matrix media upload, dual-event emit, deploy script) lives inside the OpenClaw project and its completion/tracking is OpenClaw's concern, not Concord's. If Concord's wire-format spec changes in the future, the Concord repo updates `docs/openclaw_chart_tool_spec.md` + validator + tests in lockstep, and a notification goes to whoever maintains the OpenClaw plugin — but there is no bidirectional backlog coupling.

  **Historical design decisions (retained for OpenClaw-side implementers who read this doc for context — NOT a Concord todo list):**

  1. Agents receiving `emit_chart`: ALL 9 (user directive 2026-04-08).
  2. Plugin location: `~/.openclaw/workspace/plugins/openclaw-plugin-concord-chart/` on the openclaw VM.
  3. Chart payload schema: normalized subset matching Concord's `ChartAttachment` type (see `docs/openclaw_chart_tool_spec.md` §3).
  4. Validation posture: both sides validate; lockstep discipline per the spec doc §6.
  5. No upstream PR to `openclaw/openclaw` — keep changes inside the standalone plugin.
  6. Deploy script lives in the OpenClaw project, not in Concord.
  7. Federation tolerance: dual-payload (PNG `m.image` + structured `m.text` with `com.concord.chart`) per spec doc §2.

  **Known security gotcha (not a Concord task):** `~/.openclaw/openclaw.json` stores Matrix account passwords in cleartext on the openclaw VM. Tracked separately under `~/projects/admin/openclaw-matrix-recovery/`, independent of the chart feature.

## Recent Changes
- 2026-04-09: **Native-apps sprint (Apple platforms focus) — iOS init, build script, sideload docs, iPad layout, tvOS decision recorded.** Sprint-scoped to iOS / iPadOS / tvOS only (Android + desktop out of scope). Deliverables: (1) **`cargo tauri ios init` run on orrpheus** against `src-tauri/` — generated Xcode project committed under `src-tauri/gen/apple/concord.xcodeproj/`, `concord_iOS/Info.plist`, `concord_iOS/concord_iOS.entitlements`; the `ios-entitlements.plist` + `Info.plist.template` checklist keys merged into the generated files (UIDeviceFamily=[1,2], NSLocalNetworkUsageDescription, NSBonjourServices, privacy descriptions, UIBackgroundModes=audio, multicast + wifi-info + keychain entitlements). `developmentTeam` remains `null` pending Apple ID verification. (2) **`scripts/build_ios_native.sh`** — Tauri v2 iOS build wrapper mirroring `build_macos_native.sh`, supporting `--sim` simulator mode, producing unsigned `.app` when `APPLE_TEAM_ID` is unset (Sideloadly-ready). (3) **`client/NATIVE_BUILD.md` §8a** — sideload guide covering Sideloadly + AltStore mac-companion + 7-day re-sign caveat + eventual TestFlight upgrade trigger. (4) **iPad layout** — `ChatLayout.tsx` now consumes `usePlatform().isIPad` and branches to a three-pane tablet layout at the md/lg Tailwind breakpoints; Split View / Slide Over work because the shell never pins to `100vw`. (5) **Apple TV feasibility study** committed at `docs/native-apps/appletv-feasibility.md` — Tauri v2 cannot target tvOS; recommendation is Path C (parallel SwiftUI + WKWebView shell) when the time comes post-v0.3 no earlier than Q3 2026; **no tvOS code ships this sprint**. INS-023 updated with the deferral note. Items checked in the Mobile Testing section: none yet — the pipeline is now ready for a physical-device sideload pass, but actually installing to a phone is blocked on human hands + device. Blocked out-of-sprint: sideloading onto a physical iPhone, cross-platform interop walkthrough, and the 7-condition testing protocol (all need physical hardware).
- 2026-04-22 (22:25 batch): **Two new instructions routed (INS-024, INS-025)** from `instructions_inbox.md` via orrchestrator COO pipeline. Source idea: `orrchestrator/plans/2026-04-22-22-25.md` (user-labeled "CONCORD - New Priority Feature"). Batch is distinct from the earlier 2026-04-22 01:27 batch (INS-020..INS-023) — same date, different priorities. ID-collision check passed: INS-024 and INS-025 are unused elsewhere in PLAN.md. Both added as new PRIORITY sections under Feature Roadmap, inserted between the INS-020..INS-023 native-suite block and the INS-013/018 chat-input-polish block. Contents: (1) **INS-024 (OPT-001) Sandboxed Discord bridge** — new Docker Compose service running a Matrix↔Discord bridge daemon (`mautrix-discord` recommended, `matrix-appservice-discord` fallback) in its own container with strict credential/dependency isolation from `tuwunel` and `concord-api`. Uses Matrix's existing Application Service API pattern — not a bespoke protocol. Bidirectional relay of guild messages, mentions, replies, and supported attachments. Separate docs task to write the sandbox boundary + operator runbook at `docs/bridges/discord.md`, including a commercial-scope license audit of the chosen bridge daemon. (2) **INS-025 (OPT-002) Federated server Explore menu** — surfaces the existing federation allowlist (shipped 2026-04 in `server/routers/admin.py` + `useFederation.ts` + `FederationBadge.tsx`) to non-admin users via a new read-only endpoint + an "Explore" navigation entry in the client. Each entry shows name/domain/description and a "Browse public rooms" action that reuses the existing Matrix `publicRooms` join flow. Mobile reachability coordinates with the INS-011/016/017 top-bar + bottom-pill work already in flight to avoid creating an orphaned surface. Cross-reference notes: INS-024 is strictly Matrix↔Discord bridging and does NOT interact with the INS-025 Explore menu (which stays inside the Matrix federation graph). INS-025 deliberately does NOT introduce a new discovery protocol — the allowlist is the source of truth until a broader discovery mechanism lands under the mesh roadmap. No open questions raised; routing was unambiguous.
- 2026-04-08: **All 15 open conflicts resolved in one pass** — user answered both the 2026-04-12 feedback batch and the 2026-04-22 routing batch during a develop_feature workflow invocation. **Retagged `.scope` from `public` to `commercial`** (triggers full commercial profile: CI/CD, dependency license audit, SLA error handling, input validation on endpoints, test files per new module). **Architectural revision — embedded servitude**: servitude is no longer a standalone daemon. It is an internal module of the Concord app; both mobile and desktop Concord builds ship with it embedded. Concord mobile package includes an always-online embedded server that runs whenever the user is online; Concord instances can ALSO connect to external headless servitude. INS-022 collapsed from "port headless daemon to iOS" to "expose the embedded servitude via a mobile UI toggle" — dramatically simpler. "Service Node Mode" section rewritten accordingly. **v3 commerce deferral narrow exception**: ads + ad-free tier permitted on native mobile frontends only, framed as "a donation plea" — the web UI stays free-forever with no ads. **Cross-platform framework**: default to Tauri v2 everywhere; only fall back to Flutter/RN if Tauri hits a hard wall with no workaround. **Feature parity floor**: (c) full web-client parity on native frontends, no capability deltas. **NAT traversal strategy**: all transports layered together (WireGuard + mesh + tunnels + Matrix federation) rather than one exclusively. **Apple Developer Program**: user already enrolled, ID verification in progress (up to 48h). **Monetization specifics**: PM to draft `client/MONETIZATION_PROPOSAL.md`. **Tunnel architecture**: hybrid (charter-minted AND sidekick container nodes). **Visual code editor**: research task assigned under Remaining Design Work — evaluate Blockly/Flyde/Rete.js/ComfyUI/Scratch/Node-RED against Tauri v2 + React/TS + Rust. **Geological addressing**: moved to concord-beta repo (meshes are strictly beta's domain). **Orrtellite subgroups justification**: deferred — write after 2026-04-22 priority work ships. **Orrtellite function boundary audit (INS resolution item 7)**: ran the audit — **CLEAN, no contamination**. Root concord repo contains zero orrtellite/tailscale/wireguard/headscale code references (the beta extraction in commit 56cf2f9 moved all mesh/tunnel code out; only PLAN.md / fb2p.md / naming-brainstorm.md mention these terms, all as roadmap context). **Items closed entirely**: game dev workflow cut-off sentence (user: "ignore it"), AI image gen cut-off sentence (user retracted — removed "Integrated generative AI nodes" line from concord-game-maker). All INS-020..INS-023 tasks now unblocked for the develop_feature workflow to resume clustering.
- 2026-04-22: **Four new instructions routed (INS-020 through INS-023)** from `instructions_inbox.md` via orrchestrator COO pipeline. Source idea: `orrchestrator/plans/2026-04-22-01-27.md` (user-labeled "CONCORD - TOP PRIORITY"). ID-collision check passed — INS-020..INS-023 are unused elsewhere in PLAN.md, so no suffixing required. All four grouped into a single new PRIORITY section "**Full Native Application Suite + Monetization**" inserted directly after the existing INS-014/015 Native Application Delivery section, because the batch is a direct super-set expansion of that milestone. Contents: (1) **INS-020 (OPT-001)** native iOS + Android frontends built on the existing Tauri v2 `src-tauri/` shell — feature parity with the web client explicitly NOT required; UX-layer only; reuses the orrpheus iOS pipeline proven 2026-03-28 and the MultipeerConnectivity transport scaffold. (2) **INS-021 (OPT-002)** monetization of the native frontend apps via App Store + Play Store; framed by the user as a **narrow exception** to the v3 commerce deferral — functional value stays free on the web UI, native apps are the sole paid tier. (3) **INS-022 (OPT-003)** iOS mobile servitude ("phone-as-relay") — ports the `servitude` daemon (currently headless-Linux-targeted) onto iOS with a user toggle, battery awareness, NAT traversal, and QR-pairing UX. (4) **INS-023 (OPT-004)** full five-platform suite (iOS/Android/macOS/Windows/Linux) for BOTH frontend and servitude roles; recommendation is to stay on Tauri v2 rather than re-evaluate frameworks. **Eight open questions raised** — logged under Open Conflicts > "From 2026-04-22 Routing — Unresolved": scope retag `public`→`commercial`, v3 commerce-deferral exception, Apple Developer Program enrollment, cross-platform framework choice, mobile-vs-headless servitude coexistence, native feature-parity floor, monetization model specifics, and mobile servitude NAT traversal strategy. **INS-021 is fully blocked** until the first three are resolved. INS-020, INS-022, INS-023 are partially planable but gated on the feature-floor, framework, and servitude-architecture decisions. Cross-project reuse flagged: (a) `beta/src-tauri` config for mobile bundle settings and entitlements, (b) orrchestrator's Tauri-vs-Flutter-vs-ReactNative harness-UI evaluation (consult before reopening), (c) the existing MPC transport scaffold and iOS entitlements from the 2026-03-28 orrpheus session, (d) the existing `concord-daemon`/`servitude` codebase for INS-022's mobile port. An adjacency pointer was appended to the INS-014/015 coordination note so the native-delivery story stays contiguous. No existing Roadmap items were deleted; the v3 Scope Boundary section stays in place even if the OPT-002 narrow exception is accepted.
- 2026-04-21: **Nine new instructions routed (INS-011 through INS-019)** from `instructions_inbox.md` via orrchestrator COO pipeline. Source idea: `orrchestrator/plans/2026-04-21-01-59.md`. ID-collision check passed — INS-011..INS-019 are unused elsewhere in PLAN.md, so no `C` suffixing required (unlike the 2026-04-06 INS-001..INS-007B batch). Grouped into five new Feature Roadmap sections: (1) **PRIORITY: Top-Bar Utility Relocation + Mobile Compaction** (INS-011 move bug/stats/help buttons to top bar adjacent to user icon, INS-016 collapse mobile bottom dashboard into pill row with tap-to-expand, INS-017 hide mobile utility row in focused chat view — coordinated as one workstream because all three touch top bar / `BottomNav` / mobile chrome). (2) **PRIORITY: Unified Settings Interface** (INS-012 — single navigable shell containing both `SettingsModal` user-scope and `ServerSettingsModal` server-scope settings with clear section/tab grouping, reusing existing per-tab components). (3) **PRIORITY: Native Application Delivery** (INS-014 Linux-first build pipeline on orrion using existing `src-tauri/` Tauri v2 wrapper + INS-015 machine split: orrion handles Linux/Windows, orrpheus handles macOS sign/notarize; documented hand-off protocol). (4) **PRIORITY: Chat Input + Mobile Keyboard Polish** (INS-013 auto-scroll chat pane to bottom on local message send across rooms and long histories; INS-018 mobile keyboard Enter inserts newline instead of sending — Send button only — preserve desktop Enter-to-send semantics from INS-003). (5) **OpenClaw Visual Presentation Authoring** (INS-019 — chart rendering for bar/line/pie via lightweight library, persisted as message attachments; split into INS-019a unblocked rendering/persistence half and INS-019b blocked authoring affordances pending OpenClaw definition). **One open question raised**: OpenClaw is not defined anywhere in PLAN.md, codebase, or `client/src/` — needs clarification (existing AI agent persona, planned new agent, or rename of an existing component). Logged in Open Conflicts > "OpenClaw definition gap (INS-019)". Cross-project reuse opportunities flagged: (a) `beta/src-tauri` config for native build scaffolding, (b) `orracle` for OpenClaw tool definitions if it turns out to be an orracle-hosted persona, (c) `chart.js` is the recommended chart lib unless concord already pulls another. No existing roadmap items contradicted; INS-012 should coordinate with INS-004B (which lands inside `AdminTab.tsx` reached via the new unified shell), and INS-018 must not regress INS-003's desktop Enter-to-send behavior.
- 2026-04-07: **Regression batch routed (INS-008/009/010)** from `instructions_inbox.md` via orrchestrator COO pipeline. All three describe the same defects already shipped + tester-verified on 2026-04-06 (INS-001 mobile UI, INS-002 markdown rendering, INS-003 auto-grow input). PM did NOT re-implement; instead added a new top-priority "Regression Investigation" section above the original three INS-001/002/003 entries with a 5-step triage protocol: (1) build-provenance check on `orrgate:/docker/stacks/concord/` to confirm the deployed bundle actually contains the 2026-04-06 commits; (2) cache-busting walkthrough on a real mobile device; (3) on-device repro with screen recording; (4) targeted re-open of the original entries only if Steps 1-3 confirm a real regression; (5) **add blockquote (`> text`) support to markdown rendering** — this IS new in INS-009 and was not in the original INS-002 acceptance set, so land it regardless of triage outcome. Open Conflicts updated with a "Regression Suspected" note explaining the duplication. Each of the three original PRIORITY sections now carries an inline "Regression check" subsection cross-referencing its INS-08/09/10 partner. Cross-project reuse: none — these are concord-internal client fixes. No new architecture changes.
- 2026-04-06 (later): **Seven sidebar UX instructions routed in** from `instructions_inbox.md` via orrchestrator COO pipeline, second batch same day. Added PRIORITY section "Sidebar UX Overhaul — Channels + Servers" under Feature Roadmap with items INS-001B through INS-007B. IDs suffixed `B` because the morning's routing already used INS-001/002/003 for mobile UI, markdown, and auto-grow input (see ID collision entry in Open Conflicts). Incorporated: (INS-001B) fix channel-label truncation/button overlap in `ChannelSidebar.tsx`; (INS-002B) drag-to-reorder channels and servers with backend persistence — prefer `@dnd-kit` for accessibility; (INS-003B) inline channel-tile controls + double-click-to-rename; (INS-004B) move add/delete channel UI from sidebar to admin panel, freeing sidebar space for full labels; (INS-005B) render notification badges on currently-selected channel (remove hide-on-select condition); (INS-006B) mark messages read in real time when channel is focused, to stop ghost unread counts on channel switch; (INS-007B) green presence badge on non-private servers with active users, reusing existing Matrix presence subscription. INS-001B/003B/004B all touch `ChannelSidebar.tsx` layout and must land in a single pass; INS-002B/007B both touch `ServerSidebar.tsx`. Scope: stable client only. No conflicts with existing roadmap — complements, does not replace, any other section.
- 2026-04-06: **All three PRIORITY instructions (INS-001/002/003) shipped** via orrchestrator develop_feature workflow. (1) **INS-001 Mobile UI** — added always-visible 44×44 account/logout button to the mobile top bar with an `AccountSheet` glass panel; appended a fallback Logout in `SettingsPanel`; chained `min-h-0` through `ChatLayout` mobile shell + `SettingsPanel` + `ServerSettingsPanel` so every mobile view scrolls; fixed `SubmitPage` mobile clipping (`items-start sm:items-center` + `overflow-y-auto`); added `concord-message-body` global wrap rule (`overflow-wrap: anywhere`); replaced flat `BottomNav` with a floating glass nav (sliding pill indicator, elevated center "Chat" tab with primary glow, `active:scale-95` press feedback, ≥44×44 tap targets, `cubic-bezier(0.16,1,0.3,1)` 280ms transition). (2) **INS-002 Markdown rendering** — added `react-markdown@^9 + remark-gfm@^4 + rehype-sanitize@^6` to `client/package.json`; replaced the `m.text` text renderer in `MessageContent.tsx` with a sanitized `<ReactMarkdown>` block whose schema spreads `defaultSchema`, filters `script/iframe/style/object/embed`, restricts `href` to `http|https|mailto` and `src` to `http|https`, and allow-lists `className` on styled tags so the Tailwind `components` map survives sanitization; URL extraction still runs on the raw body before parsing so `LinkPreview` continues to work for URLs inside markdown link syntax. (3) **INS-003 Auto-grow chat input** — replaced the single-line `<input>` in `MessageInput.tsx` with a `<textarea rows={1}>` driven by a `useLayoutEffect` that resets height to `auto`, measures `scrollHeight`, and caps at `min(viewport*0.4, 8 * 22px)`; toggles `overflowY` past the cap; preserves Enter-to-send / Shift+Enter / IME `isComposing` guard / Escape-cancel-edit / drag-drop / staged files / typing indicator; form root is now `flex-shrink-0` so the existing `flex-col min-h-0` parent reflows the message list automatically as the input grows. **Verification:** two parallel tester agents (static review + acceptance walkthrough) both returned PASS with no critical issues. **Files touched:** `client/package.json`, `client/src/components/chat/MessageContent.tsx`, `client/src/components/chat/MessageInput.tsx`, `client/src/components/layout/ChatLayout.tsx`, `client/src/components/settings/SettingsModal.tsx`, `client/src/components/settings/ServerSettingsModal.tsx`, `client/src/components/public/SubmitPage.tsx`, `client/src/index.css`. Stable client only (no `beta/` changes).
- 2026-04-06: **Three PRIORITY instructions routed in** from `instructions_inbox.md` via orrchestrator COO pipeline. (1) **INS-001 Mobile UI defects + redesign** — added new section under Feature Roadmap. Logout control on mobile, vertical scrolling on every page, text formatting/wrapping fixes, plus full mobile menu redesign. Targets stable client (`client/src/`), not beta. (2) **INS-002 Rich text rendering in chat rooms** — added new section. Markdown parsing (bold/italic/code/blocks/lists/links/headings) with HTML sanitization (e.g. DOMPurify), applied to every room type. (3) **INS-003 Auto-grow chat input** — added new section. Vertically auto-expanding input with synchronized message-area reflow, sensible max height, smooth transitions, preserves Enter/Shift+Enter semantics. All three are scoped to the stable Matrix-based UI. INS-001 and INS-003 should be coordinated since both touch mobile chat layout. No conflicts with existing roadmap; complements (does not replace) the existing "Mobile Dashboard" planned section, which addresses dashboard quick-actions rather than UI defects.
- 2026-04-12: **Major feedback processed** from `2026-04-12 05:33.md`. Changes: (1) Tunnel architecture: two design approaches documented (charter-minted tunnels vs sidekick nodes) — open question on which to implement. (2) Comms flow clarified: P2P handshakes for actual communication, tunnels only for virtual proximity. Local mesh + NET tunnels are complementary, not replacements. (3) Terminology overhaul: "servers" → "service nodes", management app → "servitude", naming theme = "single word descriptions of various social contracts". (4) Commerce deferred to v3 — tool-first design, UX must be perfect before economy features. (5) Game Center split into standalone sub-apps: concord-game-maker (dev environment) + concord-game-center (game console). Game-maker gets visual code editor + AI nodes. (6) Orrtellite integration: used as dependency for NET tunnels, baked into concord, end users don't need external tunneling. (7) 7 new open questions raised. (8) mbp15 corrected to always-on server, not daily driver.
- 2026-04-11 (evening): **Testing protocol refined** — added "full connection" formal definition (file exchange + text + voice + video). All 7 passing conditions now reference the full connection definition. File exchange was previously implicit; now explicit in every test condition. No new implementation work — definitional update only.
- 2026-03-28 (late night, orrion): **Orrtellite tunnel integration + reputation UI + daemon mesh wiring.** (1) WireGuard tunnel detection: new `wireguard.rs` module detects Tailscale/orrtellite mesh by running `tailscale status --json`. Auto-detects 100.64.0.0/10 CGNAT addresses in tunnel tracker → `ConnectionType::WireGuard`. `is_wireguard_address()` checks address range. `get_wireguard_status` Tauri command + dashboard status card showing mesh hostname/online peers. 5 new tests. (2) Reputation UI: TrustBadge + engagement scores in mesh map tooltips, server member trust badges, forum post author trust. (3) concord-daemon: mesh map self-registration as Backbone, sync topic subscriptions, confidence degradation task, `auto_join_places` config. (4) LICENSE confirmed present. 281 tests, 0 failures. TS clean.
- 2026-03-28 (late night, orrpheus): **iOS build pipeline + MultipeerConnectivity transport.** (1) iOS build working: `cargo tauri ios build --debug --target aarch64-sim` produces .app bundle. Both `aarch64-apple-ios` and `aarch64-apple-ios-sim` Rust targets compile. (2) Audio platform-gating: cpal + audiopus moved behind `native-audio` Cargo feature (default on, disabled on iOS/Android via target-conditional deps). Stub implementations return NoInputDevice/NoOutputDevice; VoiceEngine already handles this gracefully. (3) iOS entitlements: multicast networking, Bonjour service types (`_concord._udp`, `_concord._tcp`), microphone/camera/Bluetooth/local network usage descriptions, background audio mode. (4) MultipeerConnectivity transport: Swift `ConcordMPCManager` (250 lines) wraps MCSession + MCNearbyServiceBrowser + MCNearbyServiceAdvertiser with auto-accept, auto-invite. Rust `MpcTransport` (200 lines) implements `Transport` trait via C FFI bridge (LazyLock event buffer, extern "C" callbacks). Compiled on `cfg(any(target_os = "ios", target_os = "macos"))`. (5) Xcode deps: MultipeerConnectivity.framework, CoreBluetooth.framework, Network.framework linked. 276 tests, 0 failures.
- 2026-03-28 (night): **Reputation UI + concord-daemon mesh integration.** (1) Reputation visible everywhere: TrustBadge added to mesh map tooltips (with engagement score display), server member list (trust badges per member), forum post cards (author trust). Added `trustLevelFromRating()` and `engagementLabel()` utilities for score-to-display conversion. (2) concord-daemon wired to mesh: self-registration as Backbone node on mesh map, TOPIC_MAP_SYNC + calls topic subscriptions, periodic confidence degradation task (60s), `auto_join_places` config for auto-subscribing to place topics. (3) LICENSE file confirmed present. 276 tests, 0 failures.
- 2026-03-28 (evening): **Entry 4 Track B complete — UX revision + mobile prep.** (1) Perspective-based map navigation: new `get_perspective_view` Tauri command computes node neighborhood from any peer's viewpoint (home=live connections, remote=inferred from routes/locale). Frontend NodeMapPage rewritten: floating bubble layout centered on perspective node, breadcrumb trail for tunnel drilling, click-to-shift perspective on known nodes. Replaces old global/local toggle. (2) Forum map-view: speech bubbles overlay on mesh map showing recent posts at author positions, 3-mode toggle (hidden/map/chatroom), slide-in chatroom panel with compose. (3) Friend mesh-data sharing: MeshMapManager enhanced with friend-aware sync — 15s cooldown (vs 60s), confidence auto-upgrade on friend-sourced entries (Speculative→ClusterVerified, ClusterVerified→TunnelVerified). `syncMeshFriends` wired to dashboard startup. (4) Mobile prep: responsive initial map size, 44px touch targets on map nodes, responsive chatroom panel (full-width on mobile), fade-in animation for speech bubbles. (5) Corrected PLAN.md: voice commands are NOT mocked — fully wired to VoiceEngineHandle. Actual blocker is str0m WebRTC integration. 276 tests, 0 failures. TS + Vite clean.
- 2026-04-11: **Major feedback processed** from orrchestrator pipeline. Added: UX Language definitions (forum/place/charter/ledger/cluster/tunnel/server/node), Pragmatic Architecture (internet-first, mesh-second), 7-point Testing Protocol, perspective-based map navigation model (replaces global/local toggle), forum-as-map-crop concept, charter immutability requirement, phantom node exploration (experimental), mobile testing as top priority (iOS sideloading needed). 4 new open questions raised. Priorities shifted: mobile device testing now gates all further work.
- 2026-03-27 (late evening): **Unified identity system + wired mesh map sync into event loop.** Fixed the dual-identity problem: swarm now uses `SwarmBuilder::with_existing_identity()` with the Concord Ed25519 keypair (previously generated a random ephemeral key each startup). `NodeHandle.peer_id()` now returns the Concord hex peer_id, unifying DB/network boundary. Added `identity_keypair` field to `NodeConfig`. MeshMapManager wired into `Node.run()` event loop with 60s periodic tick, GossipSub topics `concord/mesh/map-sync` and `concord/mesh/calls` subscribed on startup. New NetworkEvent variants: `MeshMapDeltaReceived`, `CallLedgerUpdate`, `CallLedgerTombstone`. 228 tests passing, 0 failures.
- 2026-03-28: **Entry 1 all backend requirements complete (18/20).** Phase C added: location broadcasting (GeoLocation with 5-mile rounding), web portal URL generation (portal_url_for_node → *.concorrd.com), mesh map viewer API (get_mesh_map_for_viewer — prominence-sorted), mobile dashboard API (get_dashboard — peers, places, calls, last channel, portal URL). New cluster.rs module: cooperative compute (distribute_compute — capacity-weighted), fluid hypervisor (select_hypervisor + transfer hysteresis), invisible cluster voting (tally_invisibility). 267 total tests, 0 failures. Remaining 2/20 are frontend React components.
- 2026-03-27 (night): **Entry 1 Phase A+B complete.** Phase A: Extended PlacePayload with governance/ownership/whitelist, added PlaceRole/PlaceMembership types, mint_place() + build_self_registration(), blocked_peers table (schema v3), block/unblock CRUD + Tauri commands, mint_place/get_places Tauri commands. Phase B: New `governance.rs` module with VoteProposal, VoterEligibility, tally_votes(), can_perform() permission checks (14 tests). Added compute_prominence() for node ranking (2 tests). 251 total tests, 0 failures. 12 of 20 Entry 1 requirements now implemented.
- 2026-03-27 (evening): **Implemented mesh map mathematical framework** across 4 phases. New files: `concord-core/src/mesh_map.rs` (core types, address derivation, merge logic, routing, engagement, locale assignment — 16 unit tests), `concord-store/src/mesh_map_store.rs` (CRUD, tombstones, route cache, engagement counters — 6 tests), expanded `concord-net/src/mesh.rs` (MeshMapManager, sync protocol, call signals — 5 tests). Schema v2 migration adds 4 new SQLite tables. Tauri commands: `get_mesh_map_entries`, `get_engagement_profile`, `get_active_calls`. 228 total tests passing, 0 failures. All 3 design blockers resolved.
- 2026-03-27 (afternoon): Resolved all 12 open questions with user input. Added: node verification protocol (two trust tiers), reputation system (3 metrics), mesh map as decentralized database, deterministic addressing requirement, disposable anonymous nodes, call ledger lifecycle question, orrtellite for tunnels, server nodes as verification workhorses. Domain set to *.concorrd.com (placeholder). Game list finalized (8+ games). Flow-based game engine confirmed.
- 2026-03-27: Created PLAN.md. Processed `0326feedback.md` (14KB mesh architecture), `0326feedback2.md` (game center + launch animation), and concord-server concept into fb2p.md (3 queued entries).
